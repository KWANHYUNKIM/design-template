# 디자인 진화 로그 (Champion · Learnings · Scores)

> 루프는 매 RUN 이 파일을 **먼저 읽고** 시작한다: 챔피언을 이기려 하고, 학습을 반영하고, 채점을 append 한다.
> 채점 기준 = `design-rubric.md`.

## 🏆 현재 챔피언
- **RUN 16 · KELVIN** — 유지 (RUN 35 곧추 29 < 37)
- **RUN 35 소견:** 두 지시 중 **임팩트만 이행**(2→4), **완성도는 2→3**(8 RUN 연속 3 이하). 산술은 이 저장소에서 가장 정직했다 — 크리틱이 `D=198`·`A=1.0` 두 상수로 네 등급을 독립 재계산해 114.9/69.0/39.0/28.1초와 라임 띠 1156→1121→961→0을 전부 대조했고 **하나도 안 틀렸다.** 무너진 것은 계산이 아니라 **그 결과를 사람 말로 옮기는 구간**이다: 손으로 적은 845가 지면이 파생하는 844와 충돌하고(D3), 닫는 섹션이 「전화하지 않으셔도 됩니다」라 해 놓고 셋째 행에서 「물어보시면 연결해 드립니다」라 하고(D4), 845–899 등급은 갈 곳이 없었다(D5).
- **★RUN 35의 핵심 교훈 — 검사기가 못 보는 자리를 찾으라는 시험에서, 검사기를 고치지 않고 눈만 더 크게 뜨려 했다.** `verify` 0/0 · `probe` 10폭 전부 0 · `noJS` 100% · `skel` 전 슬롯 통과인데 390에서 눈으로 보니 **이미 문서에 적어 둔 함정 두 개가 재발**했다. D1은 함정 24(`.limits .lv` 에만 명시 배치 → `ln` 이 3행 1열로), D2는 특이도 사고(`tbody tr:last-child td` (0,1,3) 가 `.fold td` (0,1,1) 를 이겨 마지막 카드만 조각남). **`gridFlow` 검사는 D1에 `[]` 를 냈다** — RUN34 때 만든 폭 기반 판정이 마지막 칸이 `1fr` 을 받으면 통과시켰다.
- **★「예상 행 수」 모델은 틀렸다 — 신호는 「자동배치 자식이 명시 배치 형제 뒤에서 혼자 한 행을 차지하는 것」이다.** 처음엔 자식들의 열 span 합을 열 수로 나눠 예상 행 수를 계산했는데, `.step`/`.nrow` 처럼 **자식을 한 열에 고정해 세로로 쌓는 정상 배치를 전부 오탐**했다(run-21·24·25). 부수적으로 두 버그도 나왔다 — `grid-column: span 2` 는 span 이 **start** 쪽에 계산되는데 end 만 봤고, `1/-1` 의 `-1` 을 문자 그대로 빼서 span 을 6이 아닌 2로 셌다. 새 신호로 여섯 사례가 전부 갈렸다.
- **★검사기가 과거를 청소한 두 번째·세 번째 사례.** 새 `gridFlow` 가 **run-17의 미보고 결함**을 소급해 잡았다 — 390에서 `.am`(수수료 금액)이 3행 1열 34px 칸으로 밀려 행 번호 아래에 처박혀 있었다. 그리고 `contrast-annotation` 검사가 **주석이 있는지만 보고 값이 맞는지는 안 봤다**는 것이 D6으로 드러나, 실측 대조로 바꾸자 run-23의 세 주석(15.9→16.83 · 8.0→8.39 · 13.5→13.25)이 걸렸다.
- **★내가 D6과 똑같은 실수를 그 자리에서 또 했다.** 눈금선 색을 고치며 「라임 위 6.9:1」이라 주석을 달았는데 실측은 **5.32:1** 이었다. 대비값은 눈으로 어림하면 반드시 틀린다 — 계산해서 적어야 한다.
- **RUN 36 지시:** 짝수 = 리믹스. AD-3 유서경 슬롯(36 % 3 = 0). 노릴 칸은 **①완성도 3→5** — 9 RUN째다. 뿌리는 하나다: **「폭이 바뀌었을 때 격자가 무슨 일을 하는가」를 아무도 안 잰다.** 명시 `grid-column`/`grid-area` 를 쓰는 행은 **형제 전부를 명시**하고, **접기 미디어쿼리 안에서는 `tbody tr:last-child td` 같은 (0,1,3) 기본 규칙을 같은 셀렉터로 되받을 것.** 그리고 **접힌 표의 마지막 카드를 따로 눈으로 볼 것.** **②여백&리듬 3→5** — `rhythm` 이 6/6 만점을 주고도 1024·1440의 900px 공백을 못 봤다. 폭별 **섹션 잉크 밀도**(텍스트 면적/섹션 면적)를 재서 하위 이상치를 뱉게 하라. 도판이 행 높이를 정하는 2열 배치는 옆 칸이 비면 여백이 아니라 구멍이다.
- (이전) RUN 34 무월 25 < 37
- **RUN 34 소견:** **최저점이다.** 설득력은 올랐는데(3→4) 완성도가 내려갔다(3→2). 크리틱이 약 35개 수치를 전수 재계산해 **하나도 안 틀렸다** — 이 저장소 처음이다. 식을 먼저 선언하고 18개 점수를 상수 없이 계산하고 동점 규칙이 실제 데이터에서 발동하며 인용 두 건이 그 발동을 각각 다른 각도로 증언한다. **그런데 390~1000px 전 구간에서 그 점수가 「29 / 0」으로 두 줄로 쪼개져 있었다.** `--probe` 8종이 10폭에서 전부 0인데도.
- **★RUN 34의 핵심 교훈 — 검사기를 통과했다는 사실이 아무것도 보증하지 않는다.** D1(점수 쪼개짐)은 명시 `grid-column` 뒤에 자동배치 자식이 와서 마지막 칸이 3행 1열(35px)로 떨어진 것이고, D2(JS 끄면 배정판이 통째로 없음)는 `noJS` 검사가 `opacity:0` 개수만 세어 **「애초에 마크업에 없는 것」을 원리적으로 못 잡은** 것이다. 둘 다 검사기에 넣었고 **양방향 검증**했다 — 결함을 되심으면 잡고, 네 RUN × 두 폭에서 오탐 0.
- **★검증을 반출물로 하고 독자로 안 했다.** D3 — 기본 렌더가 그믐인데 닫는 섹션(대기자 명단)과 임세아 인용은 보름 판을 가리켰다. `?capture=1` 만 보름으로 클릭하게 해 두었으니, **제작자는 정본이 보름인 줄 알면서 독자용 기본값을 안 고친 것이다.**
- **RUN 35 지시:** 홀수 = 새 방향. AD-2 도진(35 % 3 = 2)이고 §3.5가 **주황 3연속 금지**라고 적어 두었다. 노릴 칸은 **①완성도 2→4** — 6 RUN째 막혔다. 이번엔 `--probe` 를 **만들면서** 돌리고, 조작형이 있는 섹션은 **390에서 클립 캡처로 확대해** 눈으로 볼 것(축소 컨택트시트로는 D1이 안 보였다). **②비주얼 임팩트** — RUN34는 사진 0·SVG 0·`--t-4xl` 이상 0으로 만들어 임팩트 2를 받았다. AD-2는 「하나만 크게」가 강령이니 슬롯이 맞다.
- (이전) RUN 33 사개 29 < 37
- **RUN 33 소견:** 지시 두 개를 정조준했고 **둘 다 절반만 이행됐다(각 3→4).** ①여백 — `.sheet`/`.sheet-n` 좌우 교대를 데스크톱에서 7/7 성공시켜 놓고 `@media(max-width:900px)` 한 줄로 통째로 지웠다. **390에서 가로 1종·세로 3조합** — RUN31이 3에 묶였던 바로 그 상태다. ②임팩트 — 도판을 1066×474px(뷰포트 74%)로 키웠고 선언↔구현도 좌표 검증까지 통과했지만, 390에서 촉이 43×58px로 **RUN31이 감점당한 크기보다 작았다.** 두 번 다 "데스크톱만 조준했다".
- **★RUN 33 진짜 손실은 오리지널리티 3이다 — §3.5를 봤는데도 재수렴했다.** 팔레트(마 종이→연회록·붉은→녹)와 시그니처(각주→방주)와 주제는 갈았는데, **RUN30과 최상위 CSS 클래스 13개를 공유하고 CSS 187줄 중 47줄(25%)이 완전 일치**했다. 인용 섹션은 `.intro → .qs > .q > .who + blockquote + .src` 가 통째 이식이고 셋째 인용의 출처 문구까지 같은 꼴이다. **표를 눈으로 보는 것으로는 안 지켜진다** — 구성 대장 §4에 **통과 조건 7번**(공유 클래스 5개 이하 · 동일 CSS 줄 10% 이하 · 섹션 컴포넌트 재사용 금지)을 재는 명령과 함께 넣었다.
- **RUN 34 지시:** 짝수 = 리믹스. AD-1 슬롯(34 % 3 = 1)이고 §3.5가 **크림 금지 · 공공 문서 서식 금지**라고 적어 두었다. 노릴 칸은 **①설득력 3→5** — 발명 수치는 3 RUN 연속 전부 맞는데 서술층에서 3 RUN 연속 잃었다. 착수 전에 지면이 선언할 규칙을 한 줄로 적고 **그 규칙으로 지면의 모든 추천·배치·분류를 재계산해 표와 대조**할 것(RUN33 D1은 30초면 잡혔다). **②완성도 3→5** — 5 RUN 연속 3이다. `--probe` 에 네 검사를 넣었으니 이번엔 **만들면서** 돌릴 것.
- (이전) RUN 31 여섯점 28 < 37
- **RUN 31 소견:** 노린 칸은 완성도 하나였고 **안 올랐다(3, 4연속).** 기계 점검표는 이 저장소 최고였는데 — 13폭 오버플로 0 · 하드코딩 px 0 · 대비 주석 8개 전부 실측 일치 · `.wrap` padding 사고 무재발 · 함정 12·13 준수 — 크리틱이 잡은 두 결함이 그 밖에 있었다. ①**JS를 끄면 지면이 백지**(`.rise{opacity:0}` 27개 + `noscript` 없음). ②**901~1100px에서 열 배정이 뒤집혀** 표가 4칸에 갇히고 옆이 289px 비었다. **교훈: 지시가 지목한 「지난번 실패의 좌표(폭)」를 조준했지 「실패의 종류(경계에서 배치가 뒤집힘)」를 조준하지 않았다.** 폭 개수를 늘리는 것은 이제 수확이 없다.
- **RUN 31 부수 소득:** 새로 넣은 `--probe` 의 무JS 검사가 **RUN30에서도 같은 결함을 즉시 잡아냈다**(숨은 요소 22개). 둘 다 고쳤다. 검사기에 넣은 규칙이 과거 산출물까지 청소한 첫 사례다.
- **RUN 32 지시:** 짝수 = 리믹스. 3점이 다섯 칸(여백·임팩트·모션·설득력·완성도)이라 "낮은 두 칸"이 안 먹는다. **①여백 3→5: `.sheet`/`.sheet-n` 좌우 교대.** RUN31은 세로 호흡을 6/7 다르게 짜고도 가로 판형을 7/7 동일(`40|40`)하게 두어 3에 묶였다 — RUN30이 5를 받은 그 한 줄이다. **②임팩트는 새 장치를 더하지 말고 이미 있는 도판을 제 크기로 그릴 것.** RUN31은 주제의 주인공인 점자 셀을 60×90px로 그려 옆 문단보다 작았다.
- **⚠ AD 슬롯 안에서 수렴이 재발했다:** RUN25와 RUN31이 둘 다 AD-1인데 크림 지면 + 마스트헤드 + 헤어라인 괘선 + 표 3개 + 한국어 공공 문서로 겹쳤고, `--rule2` 가 `#a9a49a`/`#a9a596` 로 사실상 같았다. 디렉터 제도는 수렴을 깨려고 만든 것인데 슬롯 안에서 다시 수렴했다. **구성 대장에 「AD별 팔레트·범주 이력」 표를 추가했다 — 다음 AD-1(34번)은 그 표를 먼저 볼 것.**
- (이전) RUN 30 스민 35 < 37
- **RUN 30 소견:** 구성 선언 관문(5단계)을 처음 통과시킨 RUN이다. 결과가 즉시 나왔다 — **오리지널리티 5**(17·18 이후 세 번째) · **여백&리듬 5**(두 RUN 연속 3이던 칸) · 크리틱의 구성 대조 **6조건 전부 통과** · `.wrap` 을 처음으로 안 씀. 관문은 작동한다. **그런데 35.** 이유는 두 가지다. ①§69가 경고한 리믹스 방어에 또 실패 — KELVIN이 5점이던 네 축 중 **셋**(임팩트·모션·설득력)을 4로 잃었다(RUN26은 둘을 잃고 32). ②완성도 3이 **세 RUN 연속**. 크리틱이 잡은 실렌더 결함 3종은 전부 `verify.mjs` 16종과 3폭 검사를 통과하고도 남았다.
- **RUN 31 지시:** 홀수 = 새 방향. **완성도 하나만** 노려라(3→5). 임팩트를 같이 올리려 하지 마라 — AD-3 편집물에서 임팩트를 억지로 올리면 여백·타이포에서 얻은 5를 도로 잃는다(크리틱 판단). 완성도의 구멍은 **"3폭만 본다"** 다: RUN30 결함 ①은 900px 경계 아래, ②는 901~1150px 사이에서 터졌고 390·1024·1440 어디서도 오버플로로는 안 잡혔다. `shoot.mjs --probe` 를 **8폭 스윕**(390·520·700·901·1024·1150·1280·1440)으로 돌릴 것.
- (이전) RUN 26 울림통 32 < 37
- **RUN 26 소견:** 리믹스 대상 선정은 **처음으로 옳았다**(RUN23·25가 낸 결론대로 총점 최고를 골랐다). 계승 4종 중 3종을 **강화**했고 — 특히 크로스오버 드래그가 다음 섹션(스윕)의 담당 유닛까지 바꾸는 **모듈 간 결합**은 KELVIN에 없던 것이다 — 폐기 2종(모듈 순서·닫는 방식)도 §52 함정을 처음으로 통과했다. 임팩트·모션은 챔피언과 동률 5. **그런데도 32.** 이유는 위 ★학습 그대로다: 챔피언이 5점이던 컬러·설득력을 각각 결함 **하나씩**으로 4로 잃었다.
- **아트디렉터 제도 2회 연속 작동:** RUN25 AD-1 don't 무위반, RUN26 AD-2 don't 무위반 + **식별 성공**. 수렴은 실제로 깨지고 있다.
- **RUN 26 2차 크리틱 (재검 · 31/40):** 1차 크리틱이 통과시킨 파일에 **다른 크리틱을 한 번 더 붙였더니 실결함 6종이 더 나왔다.** 새로 나온 것들의 성격이 1차와 다르다 — 1차는 눈에 보이는 것(대비·정렬·중복)을 잡았고, 2차는 **폴백 안의 계산**(reduced-motion에서 스크럽 분모가 음수가 되어 값이 역주행) · **라벨과 파생값의 불일치**(옥타브 점유율을 「부하 %」로 출력) · **주석이 주장하는 효과의 부재**(「화면 밖으로 흘러나감」이 820px 이상에서 −1~−432px로 지면 안에 멈춤) · **접근성 트리 삭제**(`role="img"` 이 내부 `role="slider"` 2개를 지움)를 잡았다. **한 번의 크리틱은 한 층만 본다** — 같은 파일에 성격이 다른 크리틱을 두 번 붙이는 값이 실측으로 확인됐다. 2차 지적 12종을 전부 수정해 여백 3→(호흡 분리)·설득력 3→(스펙 정합)·완성도 3→(폴백 실동작)을 손봤다.
- **RUN 27 지시:** 홀수 = 새 방향. 다만 **여백&리듬(3)·완성도(3)** 두 칸이 두 RUN 연속 3이다. 여백은 "모든 섹션이 같은 상하 패딩"이 공통 원인이니 **촘촘한 구간과 비운 구간을 의도적으로 갈라놓을 것**. 완성도는 RUN26 결함 9종 중 6종이 `verify.mjs`를 통과하고도 남았으므로 **검사기에 3줄을 추가**할 것 — ①`::before` 포함 모든 배경 위 대비 실측 ②390px에서 `td` 폭<120px 또는 행 높이>120px 경고 ③sticky 헤더가 있는데 `[id]` 앵커의 `scroll-margin-top`이 0이면 경고
- (이전) RUN 25 밤길 28 < 37
- **RUN 25 소견:** 아트디렉터 제도가 실제로 작동한다는 첫 증거가 나왔다 — don't 4종 무위반 + 크리틱이 결과물만 보고 AD-1을 지목. 그런데 **총점은 28.** 이유는 실행이 아니라 선택이다. RUN23이 이미 "리믹스 대상은 총점이 높은 것"이라고 결론냈는데 RUN25는 또 새 아카이타입을 바닥부터 세웠고, RUN17~22와 똑같은 28~32 구간에 착지했다. **새 범주는 오리지널리티를 4~5까지 밀지만 컬러·설득력·완성도를 함께 여물게 하지 못한다 — 25 RUN에서 네 번째 확인.**
- **RUN 26 지시:** 짝수 = 리믹스. **KELVIN(37)의 골격 위에서** 완성도·설득력 두 칸만 노린다. 28짜리 새 판에서 두 축을 5로 만들어도 산술적으로 32다.
- (이전) RUN 17~22 병렬 배치 최고점 32 < 37
- **RUN 17~22 배치 소견:** 6개 전부 새 범주(은행·모바일·의료·이커머스·금속·시계)에서 나왔고 **오리지널리티 5가 두 번**(17·18) 나왔다. 이 프로젝트 22개 RUN 중 처음이다. 반면 **컬러 3·완성도 3이 6개 공통** — 새 범주는 구조를 뚫지만 팔레트·마감이 함께 여물지 않는다. 다음 RUN은 17 또는 18을 **리믹스해 컬러·완성도 두 칸만** 노릴 것 (15→16에서 통한 방식).
- (원문) **RUN 16 · KELVIN** (컬러 프로파일 하우스 + 색온도 스크럽) — **37/40** ⬅ 교체 (이전: RUN 15 KERN 35)
- 이유: 직전 RUN이 지목한 약점 두 축을 **정확히 그것만** 노려서 둘 다 4→5로 올렸다. 컬러&대비·임팩트·모션·설득력 4개가 만점.
- **다음 목표:** 남은 약한 축은 **오리지널리티(4)·타이포 위계(4)·여백 리듬(4)**. 골격이 P12(KERN)의 직계 리믹스라 모듈 순서가 거의 같은 게 오리지널리티 상한. 38+ 하려면 **구조를 계승하되 순서를 깨야** 한다.
- (보존: RUN 15 KERN 35/40 · RUN 09 NOIR 34/40 — NOIR의 크롬 오브는 여전히 단일 오브젝트 임팩트 최고)

## 📈 정교화 티어 (세대별 도입 기법)
- **T1 (RUN 01~12, 완료):** 실사/CSS 비주얼 · 스크롤 리빌 · 그레인 · 단일 포인트색 · 플로팅 디테일
- **T2 (RUN 13~14, 완료):** 커서 인터랙션 · `splitting.js`식 글자 애니 · 마그네틱 버튼 · 스크롤 연동 색전환 · 숫자 카운트업
- **T3 (RUN 15 진입, 진행중):** WebGL/셰이더 배경 · 3D 틸트 · **스크롤 스크러브 타임라인(RUN15 실장 ✅)** · 뷰 전환 트랜지션
- **T4:** 아트디렉션 디테일(비대칭 그리드·오버랩·믹스드미디어) · 사운드/햅틱 힌트 · 다크/라이트 인터랙티브

## ✅ 검증된 학습 (통한 것 — 계속 쓴다)
- 헤드라인에 **강조어 1개**만(색/마커/크롬) — 과다강조는 싸구려로 보임
- **단일 포인트색 + 웜/쿨 중성 램프** = 응집력. 순백 배경 금지(크림/틴트)
- **플로팅 제품칩·배지·트러스트(별점·구독자수)** = 신뢰의 디테일이 사람티를 만�듦
- **그레인 텍스처** 한 겹 = 디지털 티 제거
- **CSS 목업/그라데이션 비주얼**이 실사보다 안정적이고 통제 가능(무인 루프에 유리)
- 알려진 함정 4종(min-width:0 · `.vis` · scrollRestoration · onerror)은 필수 — 안 지키면 히어로가 빔
- **마그네틱 버튼**은 트리거 영역을 버튼+소반경 래퍼(`.mag-zone` padding~46px)로 한정 — 부모 섹션에 걸면 가장자리에서 수백px 튐 (RUN13 크리틱)
- **파이프라인 메타(구조 Pxx · 티어 Tx · RUN NN · "발명된…")를 화면에 노출 금지** = 즉각적 AI 티. footer엔 진짜 브랜드 카피만, 추적정보는 HTML 주석으로 (RUN13 크리틱, 오리지널리티 직결)
- **T2 모션은 2종 이상 혼합**(커서+카운트업+글자리빌+마그네틱)이 리빌 단일보다 확실히 임팩트↑ (RUN13 강점)
- **크롬 표면 = conic-gradient + 느린 spin(16s) + 이동 하이라이트 스윕** = "흐르는 빛" 시그니처. 정적 원반은 임팩트↓ (RUN14 크리틱)
- **발명한 수치는 현실적으로** — 비현실 스펙(예: 헤드폰 5Hz)은 설득력+완성도 동시 감점 (RUN14 크리틱)
- **`cursor:none`은 `.js` 클래스 게이트로** — JS 실패/로드 전엔 기본 커서 폴백 유지 (RUN14 크리틱)
- **remix 시 챔피언의 핵심 시그니처를 다운그레이드 금지** — 넘으려면 계승+강화. 골격만 재사용하면 오리지널리티 정체(RUN14가 챔피언 못 넘은 이유)
- **정체를 뚫은 건 기법이 아니라 아카이타입 교체였다** — RUN13·14는 T2 기법을 늘려도 30에 묶였고, RUN15는 페이지의 종류 자체를 "타입 스페시멘/카탈로그"로 바꾸자 35로 뛰었다. 점수가 막히면 이펙트를 더하지 말고 **§A-0 아카이타입부터 바꿀 것**
- **"제품 자체가 화면인" 아카이타입은 타이포 위계가 공짜로 5점** — 활자 파운드리/포토 스튜디오처럼 콘텐츠와 매체가 같은 업종을 고르면 장식 없이도 임팩트가 나옴
- **장식이 아니라 쓸 수 있는 인터랙션 1개** — RUN15의 라이브 테스터(텍스트 입력+웨이트 슬라이더)는 리빌 10개보다 설득력이 큼. 방문자가 **직접 조작해 결과가 바뀌는** 요소를 RUN당 1개 넣을 것
- **한글 페이지엔 `word-break:keep-all`+`overflow-wrap:break-word` 필수** — 없으면 "디스플레이 산세리/프."처럼 단어 중간이 끊김. 특히 타이포를 파는 페이지에선 치명적 (RUN15 크리틱)
- **`html{scroll-behavior:smooth}`는 §D의 스크롤복원 가드를 무력화** — `scrollTo(0,0)`이 애니메이션이 되어 브라우저 복원과 경합. 초기 리셋 동안만 `scroll-behavior:auto`로 내렸다 되돌릴 것 (RUN15 크리틱)
- **웹폰트는 `<link>`/`@font-face`로 실제 로드** — `--sans:'Pretendard'`처럼 이름만 적으면 그 폰트가 설치된 내 맥에서만 보이고 남의 화면에선 폴백. 자체완결 산출물이라도 CDN 링크는 넣을 것 (RUN15 크리틱)
- **카운트업 단위는 `<span>`으로 분리 + `white-space:nowrap`** — `71개국`이 통째 텍스트면 좁은 칸에서 두 줄로 깨져 스탯밴드 베이스라인이 어긋남 (RUN15 크리틱)
- **수치와 라벨의 의미를 맞출 것** — `71개국`(국가) + 라벨 `지원 언어`(언어)는 읽는 순간 가짜 티. 발명한 스펙일수록 단위를 정확히 (RUN15 크리틱)
- **직전 RUN이 지목한 약점 2개만 정조준하면 실제로 오른다** — RUN16은 임팩트·설득력만 노려 둘 다 4→5, 총점 35→37. 매 RUN 전방위로 잘하려 하지 말고 **점수표에서 낮은 두 칸만** 공략할 것
- **컬러가 제품인 브랜드일수록 UI는 무채색으로** — KELVIN은 "UI는 무채색, 색은 오직 사진에만"을 규칙으로 박아 컬러 브랜드인데도 응집력 5점. 색을 파는 페이지가 UI까지 알록달록하면 싸구려
- **트러스트는 "제3자가 한 말"이어야 점수가 된다** — 자체 작성 사용예(KERN)는 설득력 4, 이름·직함·스튜디오·검증배지 붙은 리뷰 + 평점/건수 + 사용 크레딧(KELVIN)은 5. 내가 쓴 카피 말고 **남이 나에 대해 한 말**을 넣을 것
- **인터랙션은 "결과가 눈에 보이게" 강화** — 텍스트 테스터(KERN)보다 드래그 before/after + 강도 슬라이더(KELVIN)가 강한 이유는 조작 결과가 **색으로** 즉시 보이기 때문. 슬라이더는 값만 바꾸지 말고 화면을 바꿔야 함
- **`.wrap`과 섹션 클래스를 겸할 때 `padding` 단축 금지** — `.grader{padding:120px 0}`가 `.wrap{padding:0 40px}`의 좌우값을 덮어써 그 섹션만 40px 어긋남. `padding-top/bottom`으로 쓸 것 (RUN16 크리틱)
- **사진 위 모노 캡션에 중성 `--mut`을 쓰지 말 것** — 밝은 영역에서 안 읽힘. 밝은 값 + `text-shadow`로 분리하거나 스크림을 강화 (RUN16 크리틱)
- **타입 사이즈를 눈대중으로 찍으면 위계 점수가 4에서 막힌다** — RUN16은 10·10.5·11·11.5·12·12.5·13·13.5·14·15·16·17·18·22·28·34·40·44px로 사실상 무작위 18단계였다. 모듈러 스케일(1.25배 9단 + 디스플레이 4단)로 정리하고 **`--t-*` 토큰으로만 참조**할 것. 하드코딩 px 0개가 기준
- **기계적 스케일 치환은 위계를 평평하게 만들 수 있다** — 34px·40px가 둘 다 `--t-2xl(39)`로 흡수돼 팩 카드의 제품명과 가격이 같은 크기가 됐다. 치환 후 **"원래 A가 B보다 컸던 쌍"이 유지되는지** 반드시 대조할 것 (RUN16)
- **피그마는 페이지가 아니라 디자인 시스템을 갖는다** — 랜딩을 통째로 피그마에 옮기면 T3 모션이 전부 날아가고 편집 불가능한 프레임 덩어리가 된다. 토큰(색·타입·간격)과 재사용 블록만 피그마에 두고 페이지는 코드가 원본 (§STEP 7)
- **피그마 변수는 `setBoundVariable`로 바인딩해야 "튜닝 가능"해진다** — 변수만 만들어 두면 값 목록일 뿐이고, 명세서 노드에 바인딩해야 피그마에서 고친 게 화면에 보인다
- **"순서를 깬다"는 모듈 하나를 앞으로 옮기는 게 아니라 페이지가 닫히는 방식을 바꾸는 것** — RUN21은 스탯밴드를 히어로 앞으로 보냈지만 꼬리 4모듈(마퀴→칩+슬라이더→라이트 반전 트러스트→다크 풀스크린 CTA)이 RUN16과 동일해 오리지널리티가 3에 묶였다. 히어로만 새로 짜고 꼬리를 재사용하면 순서 파괴는 무효 (RUN21 크리틱)
- **★인터랙션의 "종류"를 직전 RUN과 대조할 것** — 조작 대상만 바꾼 같은 물건(옵션칩+슬라이더 → 가격·중량·납기 리드아웃)은 새 인터랙션이 아니다. 무엇을 조작하는가가 아니라 **무엇이 눈에 보이게 변하는가**가 달라야 한다 (RUN21 크리틱)
- **본문 최다 사용 텍스트 토큰은 정의 줄에 대비비를 실측 주석으로 남길 것** — RUN21의 `--mut:#716c62`는 다크 배경 위 3.3~3.5:1로 12px 23곳에 쓰여 AA 미달이었다. 눈으로 "회색이면 됐다" 판단하면 반드시 미끄러진다 (RUN21 크리틱)
- **`white-space:nowrap` 카운트업은 줄바꿈이 아니라 가로 오버플로로 실패한다** — RUN15의 두 줄 깨짐을 nowrap으로 막으면, 이번엔 `1fr` 트랙 + `clamp()` 폰트 조합에서 값이 셀을 넘어 구분선을 침범한다(RUN21: 981~1300px에서 최대 24px). `minmax(max-content,1fr)`로 최소폭을 보장할 것
- **클래스 겸용(`class="art hook-fig"`)은 특이도 사고를 부른다** — `.fig .art`(0,2,0)가 `.hook-fig`(0,1,0)의 `width`를 덮어써 CSS 도판이 그 물건으로 안 읽혔다. 도판은 **래퍼 안에 중첩**할 것 (RUN21 크리틱)
- **생성 직후 `390 / 1024 / 1440` 3폭 자가 대조를 합격 조건으로 박을 것** — RUN21의 결함 상위 4종(도판 붕괴·스탯 오버플로·모바일 고아 칸·사진↔캡션 불일치)은 전부 §D 함정을 다 지키고도 발생했다. §D는 만들기 **전에** 지키는 규칙이고, 이 넷은 만든 **뒤에** 봐야 보인다
- **실사 사진은 캡션이 말하는 그 대상인지 눈으로 열어 확인할 것** — RUN21은 "CNC ROOM 3축 밀링" 캡션 아래에 **석유화학 플랜트 전경**을, "PRESS LINE 80T" 아래에 **공구 걸이대**를 깔았다. 로드 검증(§D)은 "이미지가 뜨는가"만 보장하고 "그 이미지가 맞는가"는 보장하지 않는다
- **두 축을 다 올려도 베이스가 낮으면 챔피언을 못 넘는다** — RUN23은 목표한 컬러·완성도를 3→5로 정확히 올렸지만, 베이스가 CUMUL 32라 35에서 멈췄다. 리믹스 대상은 "약점이 뚜렷한 것"이 아니라 **총점이 높은 것**을 골라야 한다 (KELVIN 37을 골랐어야 했다)
- **규칙을 문서에 써두는 것과 지키는 것은 다르다** — `.wrap` 겸용 섹션 padding 단축 금지는 RUN16에서 내가 직접 EVOLUTION에 적은 규칙인데 RUN23에서 6개 섹션 전부에서 그대로 재발했다. 생성 직후 `getComputedStyle(.wrap).paddingLeft`를 실측하는 것을 합격 조건에 넣을 것
- **후보 이미지는 컨택트시트로 한 번에 눈으로 볼 것** — RUN23 후보 12장 중 4장만 주제에 맞았고, 나머지에 **콜로세움·열대 리조트·사막 도로**가 섞여 있었다. curl 200만 보고 골랐으면 RUN21의 "CNC 캡션 아래 석유화학 플랜트"가 그대로 재현됐다
- **★리믹스는 "새 축을 올리기" 전에 "계승 축을 방어"해야 한다** — RUN26은 RUN25 소견대로 총점 최고(KELVIN 37)를 골랐고 임팩트·모션을 5로 동률까지 끌었는데, **KELVIN이 5점이던 컬러·설득력을 각각 결함 하나로 4로 잃어** 32에 멈췄다. 계승 대상이 곧 챔피언의 만점 항목이므로, 리믹스 착수 전에 **"챔피언이 5점 받은 축 목록"을 적어 두고 완성 후 그 축부터 재검사**할 것
- **포인트색의 사용 한계를 토큰 주석에 써 두는 것만으로는 안 지켜진다** — RUN26은 `--k`가 종이 위 3.14:1이라 정의 줄에 "24px 이상 또는 배경으로만"이라고 박아 두고, 같은 파일에서 12px 카운터에 썼다. §60·§63에 이어 **자기가 쓴 규칙을 자기가 어긴 3회차** — 규칙은 주석이 아니라 `verify.mjs`에 넣어야 지켜진다
- **`display:block`으로 만든 표는 `width:100%`가 살아 있어 `overflow-x:auto`가 무효다** — 표가 컨테이너에 맞춰 줄어들어 스크롤할 것이 없어진다. RUN26은 390px에서 비고 열이 **89px**로 뭉개져 어절마다 줄이 바뀌었다. 스크롤은 **래퍼**에 주고 표에는 `min-width`를 박을 것
- **한 페이지 안에 같은 축의 척도를 두 개 두지 말 것** — RUN26의 스윕 막대는 로그로 차는데 눈금 라벨은 `justify-content:space-between`으로 균등 배치돼 1440에서 `250Hz`가 **81px** 어긋났다. 바로 위 SVG는 올바른 로그 위치를 쓰고 있었다. 눈금은 막대와 **같은 `pos()` 함수**로 찍을 것
- **sticky 헤더가 있으면 모든 앵커에 `scroll-margin-top`을 줄 것** — RUN26은 63px 바 아래로 착지해 섹션 머리가 통째로 가렸다. 캡처 모드에서는 바가 static이므로 0으로 되돌릴 것
- **SVG 텍스트의 `font-size` 속성도 하드코딩이다** — CSS는 `--t-*`로 깨끗한데 JS가 찍는 `font-size="15"`가 남으면 스케일 규율이 깨진다(15는 토큰에 없는 값이었다). SVG 텍스트도 클래스로 빼서 토큰을 참조하게 할 것
- **집계 수치는 표의 합과 대조할 것** — RUN26의 "누적 가동 11,200시간"은 기재된 5곳의 합과 **정확히** 같은데 바로 옆에서 "상시 설치 34곳"이라 말해, 둘 중 하나가 거짓이 됐다. 검산이 맞는 것과 **라벨이 맞는 것**은 다르다
- **SVG 다이어그램의 viewBox를 고정하지 말고 컨테이너 실폭으로 잡을 것** — RUN25는 `viewBox="0 0 1140 …"`을 박아 두어 390px에서 정류장명이 **3.7px**로 눌렸고, 세로 대안을 넣자 이번엔 820px에서 **31.5px**로 부풀었다. `viewBox` 폭 = `parentNode.clientWidth`로 두면 스케일이 항상 1이 되어 SVG 안의 글자도 타입 토큰과 같은 크기로 렌더된다. (resize에 디바운스 재그리기 필수)
- **`grid-column:1/13`처럼 칼럼 수를 박아 쓰면 모바일 폴드가 통째로 무효화된다** — `1/-1`을 쓸 것. RUN25는 6칼럼 미디어쿼리가 정상 적용되고도, 인라인 `1/13` 한 줄이 암시적 칼럼 6개를 만들어 스탯 4칸이 한 줄에 **58px씩** 뭉갰다. 미디어쿼리가 안 먹는 것처럼 보이면 먼저 `1/13`류를 의심할 것
- **UA 기본 마진이 있는 요소(`<figure>`·`<blockquote>`)를 격자 칸에 그대로 넣지 말 것** — `figure{margin:1em 40px}`가 도판 3개를 칼럼에서 40px씩 밀어내 사진만 격자에서 벗어나 있었다. 격자에 얹는 요소는 `margin` 초기화가 선행돼야 한다
- **대비비는 `--paper` 단독이 아니라 실제로 깔리는 배경 전부 위에서 잴 것** — RUN25는 §54 규칙대로 주석을 남겼는데 값이 틀렸고(`--mut` 주석 5.1 vs 실측 **4.62**), 더 중요하게 **줄무늬 행(`--paper2`) 위에서 4.22:1로 AA 미달**이었다. 지면·줄무늬·포인트틴트 3종 위 값을 전부 적을 것
- **표에 전 행이 빈 칸(“비고”)을 두지 말 것** — 4열 중 1열이 통째로 공백이면 표의 1/4이 죽는다. RUN25는 그 칸을 「지금부터 = 이 회차까지 남은 시간」으로 바꿔 표에서 가장 쓸모 있는 열로 만들었다. 모바일에서는 이 열이 가로 스크롤 뒤에 숨지 않도록 **덜 중요한 열을 접을 것**
- **`innerHTML`로 다시 그리는 인터랙티브 노드는 포커스를 복구할 것** — Enter로 고른 순간 포커스가 `body`로 떨어지면 키보드로 두 번째 선택을 할 수 없다. 재그리기 후 같은 `data-i` 노드에 `focus({preventScroll:true})`
- **클릭 타깃은 도형이 아니라 투명 `rect`로 깔 것** — 지름 10px 원만으로는 24×24 최소 타깃에 못 미치고 `<g>` 안에 데드존이 생긴다
- **`?capture=1`은 리빌·sticky만 풀면 부족하다 — `loading="lazy"`도 함께 풀 것.** 스크롤 없이 반출하면 사진이 그대로 빈 프레임으로 나간다
- **“지금”에 의존하는 페이지는 그 시간 밖의 상태까지 설계할 것** — RUN25는 “지나간 차는 흐려집니다”가 페이지의 주장인데 그 장면이 23:40~04:39에만 나타나, 하루 19시간 동안 방문자가 기능을 못 봤다. 라벨(다음 차→오늘 밤 첫차)과 상태 문장을 분기시킬 것
- **`generate_figma_design`은 캡처 모드 없이는 절반이 빈 프레임** — 리빌 섹션은 `opacity:0`, 가로 스크럽은 1/5 패널만 넘어간다. 템플릿에 `?capture=1`(리빌 강제·핀 해제·애니 정지·카운트업 고정)을 넣을 것. 그래도 생짜 덤프이므로 레이아웃 참고용 (RUN16)

- **★헤드리스 `--window-size=390` 은 모바일 검증이 아니다** — macOS 헤드리스 크롬은 창 최소 폭(~500px)을 강제해 **500px로 렌더한 뒤 390만 잘라 저장한다.** RUN29에서 390 캡처의 오른쪽이 잘려 "본문 오버플로"로 보였지만, 500px 캡처와 레이아웃이 픽셀 단위로 동일했고(같은 타입 크기·같은 줄바꿈·워드마크 폭 340px) iframe 390 실측은 `scrollWidth 375`로 넘침이 없었다. **없는 결함을 고치면 있는 디자인이 망가진다.** 모바일 폭은 `<iframe width="390">` 안에서 `contentWindow.innerWidth`·`scrollWidth`를 재거나 실제 탭으로 볼 것
- **★섹션 상하 패딩이 전부 같으면 여백 점수가 3에서 막힌다 — 두 RUN 연속으로 걸렸다** — RUN26은 다섯 섹션이 전부 `s8/s9`, RUN29는 네 섹션이 전부 `s9/s9`였다. 둘 다 간격 스케일 위에는 있었으므로 "스케일을 썼다"는 것으로는 방어가 안 된다. 채점 기준의 감점 항목이 「모든 섹션이 같은 패딩」인 이유다. **생성 직후 `grep -E '^\.(섹션)\{' 로 상하 패딩을 나열해 같은 값이 3개 이상이면 되돌아갈 것**
- **★`offsetHeight − innerHeight` 를 진행률 분모로 쓰는 스크럽은 폴백에서 반드시 음수가 된다** — `(h||1)` 은 0만 막고 **음수를 막지 못한다.** RUN26의 스윕은 reduced-motion에서 핀을 풀자 섹션 높이가 696px, 뷰포트가 773px이 되어 분모 **−77px** → 스크롤을 내릴수록 19kHz에서 32Hz로 **거꾸로** 흘렀고 최종 정착 상태가 「32Hz + 빈 막대」였다. 분모는 `Math.max(1, …)` 로 클램프하고, **핀을 푸는 폴백에서는 스크럽 계산을 아예 끄고 상태를 세로로 펼칠 것.** 리드아웃 DOM이 한 세트뿐이면 애초에 펼칠 수 없으므로 **폴백 마크업을 설계 단계에서 같이 만든다**(RUN26은 `.live`/`.fold` 두 벌로 갈라 해결 — `?capture=1` 반출물도 3문장이 전부 나가게 됐다)
- **★한 섹션에서 고친 모바일 규칙은 같은 파일의 다른 3열 그리드에도 적용해야 한다** — RUN26은 설치 원장표를 「가로 스크롤 대신 행을 세워 쌓기」로 고쳤는데, **같은 파일의 닫는 섹션(`.load li`)은 그대로 둬서** 390px에서 질문 열이 72px로 뭉개져 「바닥이 / 137kg을 / 한 점에 / 받을 수 / 있습니까?」 5줄이 됐다. `minmax(max-content,auto) 1fr minmax(max-content,auto)` + `nowrap` 리드아웃 조합은 **파일 전체에서 grep해 한 번에 접을 것**
- **★파생값은 「무엇의 비율인지」를 라벨로 검증할 것** — RUN26은 옥타브 점유율(`log2(hi/lo)/전체`)을 그대로 「부하 %」로 출력해 **18인치 우퍼를 부하 30%** 로 표기했다(전력을 가장 많이 먹는 유닛인데). 같은 양을 두 이름으로 출력하면 하나는 반드시 거짓이 된다 — 함정 9(수치↔라벨 일치)의 **파생값 판본**이다. 덧붙여 `Math.round` 한 비율 3개의 합은 99%가 되므로 **마지막 칸은 100에서 빼서** 맞춘다
- **★「화면 밖으로 흘러나간다」는 박스가 아니라 글리프로 실측할 것** — `margin-right:-12vw` + `nowrap` 좌측정렬 텍스트는 **박스만 넘치고 글자는 넘치지 않는다.** RUN26의 620px 상한은 820px 이상에서 숫자를 지면 안에 멈춰 세워(1440에서 −187px, 1920에서 −432px) 주석이 주장한 효과가 없었다. `"132"` 글리프 폭은 실측 **1.96em** 이므로 `56vw` 로 두면 항상 1.09×뷰포트가 되어 390~1960px 전 구간에서 실제로 잘려 나간다. 겹침·블리드 주장은 `Range.getBoundingClientRect()` 가 아니라 **스크린샷의 잉크 경계**로 확인한다
- **★`role="img"` 은 그 안의 인터랙티브 자식을 보조기술에서 삭제한다** — ARIA children-presentational 규칙 때문에 `role="slider"`·`aria-valuemin/max/now/text` 를 아무리 정확히 써도(RUN26은 클램프 범위와 aria 범위까지 맞춰 놨다) 노출되지 않는다. **조작 가능한 SVG의 루트는 `role="group"`**, `role="img"` 는 순수 도판에만
- **★크림 반전 밴드가 있는 페이지에서 하이라이트가 날아간 실사는 「두 번째 종이」가 된다** — `brightness()` 를 낮추는 것으로는 해결되지 않는다. RUN26 히어로는 크롭의 약 35%가 창의 순백이라 반전 밴드와 색으로 경합했다. `curl 200` → 컨택트시트 → **크롭 후 밝은 영역 비율**까지 봐야 한다. 교체본은 세로 원본을 16:9로 자를 때 `object-position:50% 62%` 로 하늘을 버려 캐비닛 면만 남겼다
- **밤/낮처럼 상태가 곧 제품인 주제는 토글이 장식이 아니라 증명이 된다** — RUN29의 낮↔밤은 전환 효과가 아니라 "비발광 간판은 밤에 안 읽힌다"를 화면으로 증명하는 장치다(꺼진 유형의 글자색이 배경과 거의 같은 값이 되어 실제로 사라진다). 조작형 인터랙션을 고를 때 **주제가 이미 갖고 있는 상태 축**(낮/밤·정지/가동·전/후)을 먼저 찾을 것. 슬라이더보다 싸고 강하다

- **★조작형 인터랙션의 근거를 주제에서 빌려오면 「값만 바뀌는 슬라이더」를 면한다** — RUN27의 지종 전환은 바탕색이 아니라 **행간·자간·괘선**을 바꾼다. 근거는 실제 조판 논리다: 얇고 잘 번지는 종이일수록 획이 서로를 침범하므로 행간을 넓혀야 한다. 조작의 결과가 **그 업의 물리를 따르면** 장식이 아니라 설명이 된다. 반대로 같은 UI라도 결과가 임의로 정해진 값이면 감점이다
- **★자가 대조 6문항은 실제로 불합격을 낸다 — 형식적으로 통과시키지 말 것** — RUN27은 5번(제3자 트러스트)에서 걸렸다. 장인 구술 인터뷰가 있어서 "사람 말이 있으니 됐다"고 넘어갈 뻔했지만, **구술은 자사의 말이지 남이 나에 대해 한 말이 아니다.** 실명·소속·날짜·출처가 붙은 인용 3건을 따로 넣어 해결했다. 여섯 문항 중 하나라도 "아마도"면 그건 아니오다
- **편집디자인 계열은 임팩트가 구조적으로 낮게 나온다 — 알고 설계할 것** — RUN27은 일곱 칸이 4~5인데 임팩트만 3이다. 조용한 것이 유서경 AD의 의도지만 채점표는 "스크롤을 멈추게 하는 장면"을 묻는다. 명조 지면에서 임팩트를 벌려면 **큰 사진 한 장이나 지면을 가르는 단 하나의 장치**가 필요하다. 타입만으로는 4가 상한이다

- **★검사기의 「신호」를 잘못 고르면 정상을 잡고 결함을 놓친다 — 신호는 결함의 *피해 모양*에서 뽑아야 한다.** 함정 24를 잡으려고 두 번 시도했다. ①RUN34식 **폭 기반**(마지막 칸이 1열·트랙폭으로 밀림) → RUN35 D1을 놓쳤다. 밀려난 칸이 `1fr` 을 받아 폭이 넓었기 때문이다. ②**예상 행 수**(span 합 ÷ 열 수) → D1은 잡았지만 `.step`/`.nrow` 처럼 **자식을 한 열에 고정해 세로로 쌓는 정상 배치를 전부 오탐**했다(run-21·24·25). 맞는 신호는 셋째였다 — **「자동배치 자식이, 열을 못박은 형제 뒤에서, 혼자 한 행을 차지한다」.** 이게 sparse 커서에 밀린 결과의 실제 모양이고, 여섯 사례를 전부 갈랐다
- **★계산된 스타일에서 grid span 을 읽을 때 두 가지가 함정이다** — `grid-column: span 2` 는 span 이 **`grid-column-start`** 쪽에 계산되고 end 는 `auto` 다(end 만 보면 span 을 통째로 놓친다). 그리고 `1/-1` 의 `-1` 은 문자 그대로 빼면 안 된다 — 열이 N개면 선은 N+1개이고 `-1` 은 N+1번 선이다
- **★「주석이 있는가」를 재는 검사는 틀린 숫자를 통과시킨다 — 값을 대조해야 한다.** `contrast-annotation` 은 `:root` 텍스트 토큰에 대비 주석이 **달려 있는지**만 봤다. RUN35는 9.11·12.6이라 적고 실제는 9.84·13.97이었으며, 게다가 `:root` **밖**(`.elsewhere .fine`)에 선언한 색이라 사정권 밖이었다. 실측 대조로 바꾸니 run-23의 세 주석도 걸렸다. 대조할 때 **한 토큰이 낮/밤 테마로 두 번 정의**될 수 있고(마지막 값만 잡으면 어두운 배경끼리 재게 된다 — run-29), **「4.5:1을 넘겨야 한다」는 측정 주장이 아니라 요구 조건**이다(run-19 오탐)
- **★대비값은 눈으로 어림하면 반드시 틀린다 — D6을 고치는 그 자리에서 내가 또 틀렸다.** 눈금선 색을 바꾸며 「라임 위 6.9:1」이라 적었는데 실측은 **5.32:1** 이었다. 색을 바꿀 때마다 계산해서 적는다
- **★도구가 남기는 쓰레기도 파이프라인의 일부다** — `shoot.mjs` 가 실행마다 `/tmp/shoot-profile-<PORT>` 를 만들고 안 지웠다. 175개 × ~10MB = **1.7GB** 가 쌓여 디스크를 0바이트로 만들었고, **Bash·Edit·Write 가 전부 막혀 루프가 멎었다**(비우려면 명령이 필요한데 명령을 돌리려면 공간이 필요한 교착). 이제 시작할 때 남은 것을 쓸고 `finally` 에서 자기 것을 지운다
- **★탑처럼 긴 도판이 행 높이를 정하는 2열 배치는 옆 칸에 구멍을 낸다** — 도판을 줄이면 임팩트가 사라지므로, **옆 칸을 `position:sticky` + `align-self:start`** 로 붙여 도판을 따라 내려오게 한다. 스크롤 실측으로 확인할 것(전체 페이지 캡처는 sticky 를 시작 위치로 찍어 구멍이 과장돼 보인다)
- **★바닥 근처를 가리키는 표시물은 「표시선」과 「라벨」을 나눠라** — 한 덩어리로 두면 제 높이의 절반만큼 바닥에서 못 내려가 서로 다른 값이 **같은 자리로 클램프**된다. RUN35는 105(지상 1.5m)와 180(4.5m)이 390에서 똑같이 붙었다. 3px 선은 정확한 지점에, 라벨은 그 위에 가둔다 → 8.8/17.1/54.9/281.5px 로 갈렸다

## ⚠️ 약점 패턴 (반복 방지)
- **골격 수렴 — 27장 실측.** 마지막 섹션이 전환 블록 24장 · 증언 섹션 20장 · IO+`.rise` 리빌 27장 ·
  「증언→가격/FAQ→닫기」 3연이 최근 12장 중 8장. 이 항목은 **문장으로 세 번 적혔다가 세 번 무시됐고**
  (여기 · RUN21 교훈 · SKILL "점수가 막혔을 때"), 그래서 루프 **⑥ 구성 선언 관문**이 됐다.
  대장과 통과 조건 6개: `.claude/skills/design-loop/references/composition.md` — **RUN 끝나면 갱신할 것**
- 콘텐츠 아카이타입 고정(hero→기능3→리뷰) → 매번 아카이타입 바꿀 것(§A-0)
- 리빌이 스크롤에 과의존 → 로드 폴백 필수
- 모션이 리빌 한 종류뿐 → 호버/커서/카운트업 등 2종 이상 섞기(T2)

- **★구성 선언 관문(5단계)은 실제로 오리지널리티를 올린다** — RUN30이 처음 통과했고 오리지널리티 5·여백 5·구성 대조 6/6이 나왔다. 「꼬리를 재사용하지 마라」는 세 번 문장으로 적혔다가 세 번 무시됐는데, **만들기 전에 통과해야 하는 관문으로 옮기니 한 번에 지켜졌다.** 규칙은 조언이 아니라 관문이나 검사기로 넣어야 지켜진다 (§80과 같은 결론)
- **닫는 방식은 형식이 아니라 "누구를 향하는가"로 세야 한다** — 26「탈락 체크리스트」와 29「우리가 만들지 않는 것」은 형식이 달라 보이지만 둘 다 *구매자를 거르는* 닫기다. 연속 두 번이면 그것도 수렴이다. RUN30은 방향을 뒤집어 *자사의 실패를 공개하는* 쪽으로 닫았다
- **좌우 여백을 섹션마다 교대시키면 여백 점수가 3→5로 간다** — RUN30은 `.sheet`(좌40·우80)와 `.sheet-n`(좌80·우40)을 번갈아 쓰고 상하 패딩을 40/56·120/120·56/40·80/80 네 호흡으로 갈랐다. 「모든 섹션이 같은 패딩」은 두 RUN을 3에 묶었던 사유다
- **★리믹스 방어 실패가 3연속이다** — RUN26은 챔피언의 5점 축 4개 중 2개를, RUN30은 **3개**를 4로 떨어뜨렸다. §69를 적어 두고도 또 당했다. 리믹스 착수 시 **"챔피언이 5점 받은 축"을 파일 맨 위에 적어 두고, 완성 후 그 축부터 크리틱에게 먼저 물을 것**
- **`chrome --headless --window-size=390` 은 창 최소폭 500px로 클램프된다** — 390 캡처라고 믿은 것이 500에서 렌더한 뒤 잘린 그림이고, 잘린 가장자리가 가로 오버플로처럼 보인다. RUN30에서 없는 결함을 쫓다 시간을 버렸다. **`scripts/shoot.mjs`(CDP `Emulation.setDeviceMetricsOverride`)로만 모바일을 검증할 것**
- **3폭(390·1024·1440)만 보면 그 사이가 빈다** — RUN30 결함 둘이 900px 경계 아래와 901~1150px 사이에서 터졌다. `shoot.mjs --probe` 를 **8폭 스윕**으로 돌릴 것. probe는 가로 오버플로 외에 **각주 참조↔본문 거리**와 **한 줄 안 그리드 칸 높이 편차**도 잰다 (둘 다 RUN30 크리틱이 찾은 결함 유형)
- **검산은 숫자에서 문장으로 넘어가야 한다** — RUN30은 발명 수치 8종이 **하나도 안 틀렸는데**(두 RUN 연속) 설득력 4였다. 남은 실패는 전부 서술 층이었다: 지면이 선언한 규칙과 파생값이 반대, "마지막 장에 적었습니다"가 회수 안 됨, 인용이 지면과 모순, "다섯 중 셋/둘" 분류가 표와 불일치, "두 번째 줄"이 실제로는 세 번째 줄. **ⓐ선언한 규칙으로 파생값 재계산 ⓑ내부 참조("뒤에·마지막 장에·n번째 줄")를 전부 목록으로 뽑아 실제 위치 확인 ⓒ분류 개수를 표에서 다시 세기**

- **★검사기에 넣은 규칙은 과거 산출물까지 청소한다** — RUN31이 무JS 검사를 `--probe` 에 넣자 **RUN30에서도 같은 결함(숨은 요소 22개)이 즉시 잡혔다.** 크리틱이 RUN30에서 "JS 미실행 시 정적 옛값"을 지적했을 때 정적값만 채우고 `.rise{opacity:0}` 은 그대로 뒀던 것이다. **지적을 문자 그대로만 고치면 같은 병의 몸통이 남는다 — 검사로 옮겨야 뿌리가 뽑힌다**
- **리빌은 `.js` 게이트 뒤에 둘 것** — `.rise{opacity:0}` 을 무조건 걸면 스크립트가 꺼진 브라우저에서 지면이 통째로 백지가 된다. `<html class="js">` 를 페인트 전에 붙이고 `.js .rise{opacity:0}` 로 쓴다. `<noscript>` 보다 확실하고 FOUC 도 없다
- **★"지난번 실패의 좌표"가 아니라 "실패의 종류"를 조준할 것** — RUN30 지시는 "901~1150px 에서 터졌으니 8폭을 돌려라"였고 RUN31은 13폭을 돌려 오버플로를 완전히 잡았다. 그런데 같은 구간에서 **다른 종류**(넓은 트랙이 좁은 콘텐츠에 감)로 또 터졌다. 폭을 더 보는 것은 수확이 끝났다 — **재는 항목을 늘려야 한다**
- **미디어쿼리에서 두 칸을 좁힐 때 열만 바꾸면 읽기 순서가 뒤집힌다** — 넓은 콘텐츠에 넓은 트랙을 주려고 `grid-column` 을 맞바꾸면 DOM 순서와 시각 순서가 어긋난다. **한 칸으로 세우는 것이 정답이다**
- **격자 괘선은 반드시 콘텐츠 뒤로(`z-index`)** — 앞에 두면 사진과 헤드라인 위를 선이 가로지른다. "격자를 보이게 두는 것"과 "콘텐츠를 가로지르는 것"은 다르다 (RUN31)
- **두 칸 나란한 자리는 `grid-row` 를 명시할 것** — `margin-top` 으로 띄우면 좌우 기둥의 시작 높이가 서로 달라진다. RUN31에서는 그것이 페이지 주제(읽기 순서)와 직결돼 자체 검사가 실제로 어긋남 2곳을 리포트했다
- **도판이 "실제 비율"이라고 쓰려면 비율을 계산해서 그릴 것** — RUN31은 「실제 치수 비율로 그렸습니다」라고 적고 선언 1:1.667 을 1:2.308 로 그렸다. 점 지름을 `calc(var(--pitch)*.6)` 으로 묶어 선언과 구현을 한 값에 연결했다
- **조작형의 상태가 on/off 한 비트면 "런타임 계산"이 하드코딩과 구별되지 않는다** — RUN31은 어긋남 개수를 좌표에서 진짜로 계산했는데 전 폭에서 4로 고정이라 화면이 그 사실을 증명하지 못했다. **배치를 바꾸는 선택지를 붙여 4→2→0 으로 움직이게 하자 비로소 계산이 보였다**

- **★검사기가 과거 산출물을 청소한 두 번째 사례** — RUN33 크리틱이 「여백은 모바일에서 재라」고 하자 `--probe` 에 판형·호흡 검사를 넣었고, 그 즉시 **RUN30(가로 1종)과 RUN31(가로 1종·세로 3조합)이 걸렸다.** RUN30은 여백 5를 받았는데 그건 데스크톱 얘기였다. 셋 다 고쳤다. §127에 이은 두 번째 — **지적을 검사로 옮기면 그 RUN만이 아니라 저장소 전체가 올라간다**
- **★데스크톱에서 갈라 놓은 여백은 모바일 한 줄로 무효가 된다** — `@media(max-width:900px){.sheet,.sheet-n{padding:…같은 값}}` 이 7/7 교대를 통째로 지운다. 문서의 주 뷰포트는 390이다. **여백을 갈랐다고 말하려면 390에서 재라**
- **★같은 AD 슬롯의 재수렴은 팔레트를 바꿔도 막히지 않는다** — RUN33은 §3.5 표를 보고 바탕색·포인트색·시그니처를 전부 갈았는데도 오리지널리티 3이었다. **골격(CSS 클래스·섹션 컴포넌트)이 물려받아졌기 때문이다.** 눈으로 보는 대조는 세 번째로 실패했다 — 구성 대장 통과 조건 7번으로 옮기고 `comm -12` 로 재게 했다
- **조작형 리드아웃은 상수가 아니라 도형에서 파생시킬 것** — RUN33은 `VIEW` 객체에 값을 박아 세 상태에서 두 칸이 안 변했다. `path` 의 `d` 좌표를 파싱해 뿌리·끝·길이·기울기를 계산하도록 바꾸자 **그 자리에서 파싱 인덱스 오류가 드러났다**(1:12·240mm 로 출력). 하드코딩이었으면 안 보였을 것이다. **값을 적어 두면 계산인지 아닌지 구별되지 않을 뿐 아니라, 틀려도 모른다**
- **고정 `viewBox` 안의 글자는 토큰화로 안 풀린다** — 실렌더 = `font-size × (렌더폭 ÷ viewBox폭)`. RUN33은 `var(--t-lg)` 를 썼는데 390에서 6.0px 였다. **폭 구간별로 사용자 단위를 올려야** 한다(≤900에서 `--t-3xl` → 14.6px). RUN25 §85가 학습으로만 남아 재발했다 — 이번엔 `--probe` 의 `svgIssues` 가 잰다
- **SVG 는 상태를 바꾼 뒤에도 `viewBox` 밖을 재야 한다** — RUN33의 「푼 것」은 `translateX(170)` 로 부재 우단이 1030 (>900) 이 되어 외곽선이 잘렸다. 초기 렌더에서는 안 나온다

- **★`--probe` 8종 전부 0 이 아무것도 보증하지 않는다** — RUN34는 10폭에서 8종 전부 0이었는데 **주 뷰포트 390에서 이 지면의 핵심 숫자가 두 줄로 쪼개져** 있었다. 검사기는 "잡도록 만든 것"만 잡는다. 매 RUN **검사기가 안 보는 자리를 하나는 새로 찾아 넣을 것**
- **★명시 `grid-column` 을 가진 자식 뒤에 자동배치 자식을 두지 말 것** — sparse 배치가 커서를 명시 칸 뒤로 보내 **마지막 자식이 다음 행 1열(트랙 하나 폭)로 떨어진다.** RUN34는 세 자리 점수가 「29 / 0」이 됐다. 두 칸 이상 쓰는 행은 **전부 `grid-area` 로 자리를 명시**할 것. `--probe` 의 `gridFlow` 가 잰다
- **★무JS 검사는 `opacity:0` 개수가 아니라 「보이는 텍스트 길이 비」로 잴 것** — 개수만 세면 **JS 가 만들어 넣는 내용이 애초에 마크업에 없는 경우**를 원리적으로 못 잡는다. RUN34의 배정판이 그랬고 판정은 「OK」였다. 조작형의 **기본 상태를 정적 마크업으로 먼저 그려 두고** JS 가 이어받게 할 것
- **★검증을 반출물로 하지 말고 독자로 할 것** — RUN34는 `?capture=1` 에서만 정본(보름) 판을 클릭하게 해 두어, **반출물은 맞고 독자가 보는 첫 화면은 자기 인용문과 닫는 섹션을 부정**했다. 캡처 모드를 손볼 일이 생기면 그건 기본값이 틀렸다는 신호다
- **`opacity` 로 흐리면 합성 대비가 무너진다** — RUN34의 `.row.out{opacity:.55}` 는 `--mut` 를 **2.60:1** 로 만들었다(AA 4.5 미달). 토큰 대비만 재고 **합성 상태를 안 쟀다.** 흐리려면 투명도가 아니라 **색을 직접 낮출 것**
- **관문 조건도 틀릴 수 있다 — 재는 방법을 먼저 검증할 것** — 통과 조건 7번의 "동일 CSS 줄 10% 이하"를 줄 단위로 재니 RUN34가 22%로 걸렸는데, 겹치는 줄이 **거의 전부 규격이 강제하는 것**(토큰 스케일·스크롤 가드·리빌 게이트·capture·fold·리셋)이었다. **규격을 지킬수록 관문에 걸리는 모순.** `scripts/skel.mjs` 로 보일러플레이트를 빼고 세니 RUN31↔34는 1.0%(무죄), RUN30↔33은 12.2%(유죄)로 갈렸다
- **§3.5 에 적힌 금지가 또 무시됐다** — RUN34는 「공공 문서 서식 금지」가 명시돼 있었는데 「표 1 · …」 caption 관용구가 RUN31과 **글자 그대로 같고**, AD-1이 25·31·34 **연속 3회 한국어 기관 문서**였다. 표면 금지(색·범주)로는 안 걸린다 — **장르와 관용구 수준으로 금지를 적을 것**

## 🧾 점수 로그 (RUN · 합계 · 강점 → 개선점)
| RUN | 점수 | 강점 | 다음에 개선 |
|---|---|---|---|
| 35 곧추 GOTCHU | **29**/40 | **선언 → 파생이 이 저장소에서 가장 정직했다** — `D=198`·`A=1.0` 두 상수만으로 크리틱이 네 등급을 독립 재계산해 114.9/69.0/39.0/28.1초 · 삼각 프로파일 전환 조건 `v²/A>D` · 라임 정속 픽셀 1156→1121→961→**0** · 표 검산(37/412=8.98%, 14/37=38%)이 **전부 일치** / **임팩트 2→4** — 1180px 형광 라임 슬래브 + 1080을 누르면 그 슬래브가 통째로 사라지는 두 번째 장면 / 닫기 「경쟁사를 추천」은 미사용 행을 실제 소진하고 「전화하지 않으셔도 됩니다」라는 행동 지시까지 새로 만듦 · 저장소 첫 형광계 / 구성 관문 **7/7**(skel ⓐ0 ⓑ0.0% vs 29) · AD-2 don't 0 · 식별 성공 | 결함 13종 수정완료 — **D1 함정 24 재발**(`.limits .lv` 에만 명시 배치 → `ln` 이 3행 1열 230px로. **`gridFlow` 검사가 `[]` 를 냈다**) · **D2 특이도 사고**(`tbody tr:last-child td` (0,1,3) > `.fold td` (0,1,1) → 마지막 카드만 3px 괘선으로 조각남) · D3 손으로 적은 845 ↔ 파생 844 · D4 닫는 섹션 자기모순 · D5 845–899 공백 + 「어디로」 라벨 부재 · D6 다크 대비 주석 2개 오기(9.11→9.84 · 12.6→13.97) · D7 「중간을 비웠다」 선언이 거짓(11단 중 10단 사용) · D8 데스크톱 공백 붕괴(탑 1180 vs 리드아웃 700) · D9 폴백에서 `.flat` 전환 미차단 · D10 390에서 캡슐이 105·180 같은 자리로 클램프 · D11 무JS 도판만 눈대중(1.5%/97% ↔ 파생 0.77%/98.45%) · D12 눈금선 라임 위 1.65:1 · D13 14.1 ↔ 14.07 · **추가 자체 발견: 390 확대에서 「6,000 kg」이 수치/단위 두 줄로 쪼개짐**(6em 열 → `max-content`). **약점: 완성도 3(8연속) · 여백&리듬 3 · 타이포 3** |
| 34 무월 MUWOL | **25**/40 | **발명 수치 약 35종을 크리틱이 전수 재계산해 하나도 안 틀렸다 — 저장소 처음** / 식을 먼저 선언하고(등급×50+달적합×30+대기분기×5) 18개 점수를 상수 없이 파생 · **보름에서 동점 200이 실제로 발동해 대기 분기가 가른다** · 인용 2건이 그 발동을 각각 다른 각도로 증언하고 수치까지 맞는다 / 구성 대장 **7조건 전부 통과**(skel.mjs ⓐ1 ⓑ1.0% ⓒ0) / 닫기 「대기자 명단」은 미사용 행 · 밤하늘+시안은 저장소 첫 청록계 · AD-1 don't 무위반 + 식별 성공 | 결함 10종 수정완료 — **D1 치명: 390~1000 전 구간에서 점수가 「29/0」으로 쪼개짐**(명시 grid-column 뒤 자동배치 → 마지막 칸이 3행 1열 35px). `--probe` 8종이 10폭에서 전부 0인데 통과했다 · **D2: JS 끄면 배정판이 통째로 없음**(noJS 검사가 opacity 개수만 세어 못 잡음) · **D3: 기본 렌더(그믐)가 닫는 섹션·인용과 모순** — capture 모드만 보름으로 고쳐 뒀다 · D4 `opacity:.55` 합성 대비 2.60:1 · D5 범례 칩이 해칭을 못 받음 · D6 죽은 트랜지션(innerHTML 재생성) · D7 띠 폭이 손으로 적힘 · D8 표2 라벨/주어 불일치 · D9 동점군 덮어쓰기+사유 하드코딩 · D10 죽은 셀렉터. **약점: 완성도 2(역행) · 임팩트 2(사진0·SVG0·d1 이상 0) · 타이포 3(7단계, 모바일 h2가 본문과 한 단계) · 오리지널리티 3(§3.5 「공공 문서 서식 금지」 위반)** |
| 33 사개 SAGAE | **29**/40 | 도판을 1066×474px(뷰포트 74%)로 키우고 **선언↔구현을 좌표 검증까지 통과**(뿌리80·끝160·길이120·기울기 1:3·18.435° 전부 일치 — RUN31의 1:1.667/1:2.308 사고 없음) / 발명 수치 12종 전원 검산 통과 · 제3자 3건에 자격번호·권호·쪽수 / 구성 대장 **6/6** · 닫기 「만든 사람들의 이견」은 미사용 행 / 연회록 바탕 + 짙은 녹 — 저장소 첫 색상계 / AD-3 don't 명시 위반 0 · 식별 성공 | 결함 10종 수정완료 — **D2 함정 12 그대로 재발**(색칩 span+이름 → 390에서 이름이 라벨 열로. **파일 주석에 준수를 주장하면서 어겼다**) · D1 방주가 표2와 삼중 모순(육송 비 2.03이 최대인데 「참죽이나 육송」) · D3 SVG 캡션이 viewBox 밖으로 잘림 · D4 390에서 도판 글자 6.0px · D5 「푼 것」에서 부재 외곽선이 viewBox 밖 · D6 인용/라벨 지시 어긋남 · D7 리드아웃 하드코딩 → **path 좌표 파생으로 교체하자 파싱 오류가 드러남** · D8 aria-live·SVG title 고정 · D9 스케일 밖 px. **약점: 오리지널리티 3(RUN30과 CSS 25% 동일) · 설득력 3(3연속) · 완성도 3(5연속)** |
| 31 여섯점 YEOSEOT | **28**/40 | 컬러 5(대비 주석 8개 **전부 실측 일치**) · 오리지널리티 4 / **구성 대조 6/6** · 닫는 방식 「용어 사전」은 대장 미사용 행 · 증언 블록 없이 제3자를 3번째 기록표로 대체 · 히어로 0 · CTA 0 / 발명 수치 **16종 전부 검산 통과** · 표 1이 거부한 지적 1건과 사유까지 실음 / 조작형이 좌표에서 **진짜 계산** — 크리틱이 독립 재계산해 완전 일치 확인 / AD-1 don't 무위반 + 식별 성공 | 결함 17종 수정완료 — **①JS 끄면 지면 백지**(`.rise` 27개, 잉크 0px) → `.js` 게이트로 전환, **같은 수정을 RUN30에도 역이식** · **②901~1100px 열 배정 역전**(표 258px, 옆 289px 구멍) → 한 칸으로 세움 · 「바뀐 것은 하나」↔개정 이력(둘) 충돌 · 「실제 치수 비율」이 1:1.667 선언에 1:2.308 구현 · 「비슷해 보입니다」가 82px 차이와 불일치 · 부록 B 이중 지시 · 견본 판이 자기 괘선을 4px 빗나감 · capture 에서 검사 미작동 · 어긋남 값이 전 폭 고정 → **배치 전환 3종(4→2→0) 추가** · 평량 사유가 후행 기준 인용 · 「다섯 단계」↔실측 6단계 · `.dims` 단위 79px 어긋남 · 표1 시점 무순 · 용어 무순 · 數表→數標 · generic 롤 + focusin 단방향 · 죽은 CSS. **약점: 완성도 3(4연속) · 여백 3(가로 판형 7/7 동일) · 임팩트 3 · 모션 3 · 설득력 3** — 3점이 다섯 칸 |
| 30 스민 SMIN | **35**/40 | **타이포 5 · 컬러 5 · 여백 5 · 오리지널리티 5** — 이 저장소 최초의 4개 5점 조합 / **구성 선언 관문 첫 통과**(6조건 전부) · 증언을 2번째로 올려 꼬리 구조를 갈랐고 `.wrap` 을 처음 폐기 / 닫는 방식 신규 「올해 버린 물」 자사 폐기 대장(버튼 0개) / 대비 주석 7개가 **실측과 전부 일치**(§80 이후 처음) / 발명 수치 8종 검산 전원 통과 · L\*a\*b\* 6개가 hex와 정확히 일치 / 드래그+버튼+방향키 3경로 재정렬 → 3색 띠 재배치 + 최종 색·색좌표·견뢰도 동시 변화 / AD-3 don't 무위반 + **식별 성공** | 결함 13종 수정완료(≤900px 표 색이름 셀 붕괴 — `tr:block`+`td:flex` 가 크롬에서 셀 텍스트를 잃음 · 901~1150px L\*a\*b\* 2줄 깨짐 · 사진3 중국어 상호 간판 노출 · 각주가 참조에서 3,357px 떨어져 관측 불가 · 겹침 견뢰도가 자기 규칙과 역주행 · 「마지막 장에 적었습니다」 미회수 · 산지 표기 자기모순 · 폐기 분류 개수 불일치 · 행 번호 오지시 · JS 미실행 시 정적 L\*a\*b\* 옛값 + `<noscript>` 부재 · 토큰 밖 하드코딩 색 · 표 번호 체계 반만 적용 · 표제면 우측 441px 구멍). **약점: 완성도 3(3연속) · 임팩트 4 · 모션 4 · 설득력 4** — 리믹스 방어 3축 실패 |
| 13 SIGNAL | **30**/40 | 스플릿 다이프틱 임팩트·응집력 / T2 모션 5종 실장 | 마그네틱 트리거 스코핑(수정완료) · footer 메타 제거(수정완료) → 다음엔 오리지널리티/완성도 +2 노려 34 돌파 |
| 14 RESON | **30**/40 | 마그네틱 스코핑·로드폴백 실장 / 오디오 서사·리뷰 설득력 | 크롬 회전+스윕(수정완료)·20Hz 현실화(수정완료)·커서 폴백(수정완료). remix가 골격 재사용→오리지널리티 정체. **2연속 챔피언 미달** |
| 16 KELVIN | **37**/40 🏆 | 컬러&대비 5·임팩트 5·모션 5·설득력 5 / 직전 약점 2축을 정조준해 둘 다 4→5 / 색온도 스크럽(warm→cool)·드래그 before-after 그레이더 / 콜로리스트 리뷰+평점+크레딧으로 제3자 신호 확보 | 결함 5종 수정완료(사진 위 캡션 가독성·히어로 스크림·`padding` 단축이 `.wrap` 좌우 덮어씀·스튜디오명 오타·스펙밴드 정렬). **남은 약점: 오리지널리티 4 — 골격이 P12 직계 리믹스라 모듈 순서가 거의 동일**. RUN17은 홀수=새 방향, 구조 순서부터 깰 것 |
| 21 TANSO | **28**/40 | 타입 스케일 규율 최고(하드코딩 px 0·font-size 48개 전부 `--t-*`) / 발명 수치의 상호 정합(품번 합=표준품 수, 리뷰어=크레딧 업체, MOQ 3곳 일치) / 신규 구조 P14 + 선(先)스탯 밴드 | 결함 9종 수정완료(후크 도판 특이도 붕괴·스탯밴드 가로 오버플로·모바일 고아 칸·본문 대비 3.3→5.0:1·**캡션과 다른 사진(석유화학 플랜트)**·중량 공식 11% 과다·슬라이더가 자사 공시 초과·reduced-motion 2건). **남은 약점: 오리지널리티 3 — 꼬리 4모듈이 RUN16과 동일** → RUN22는 페이지가 닫히는 방식부터 폐기할 것 |
| 23 들물 | **35**/40 | **목표 2축 달성: 컬러 3→5 · 완성도 3→5** / 무채색 슬레이트 + 고시인성 옐로 1색을 안전 신호에만 사용(장식 금지), 토큰 정의 줄에 대비비 실측 주석 / **인터랙션의 종류를 바꿨다** — 칩+슬라이더 리드아웃이 아니라 시간을 끌면 단면도에 물이 차오르고 귀환 한계가 재계산됨 / 09:00에 "조위는 낮은데 귀환로는 이미 116cm 잠김"이 화면으로 증명됨 | 결함 3종 자체 발견·수정(**.wrap 겸용 섹션 padding 단축 — 내가 쓴 규칙을 내가 어김** · "발목" 카피가 조위 326cm와 불일치 · 단면 지형이 배경과 동일 밝기로 형태 안 읽힘). **베이스가 32라 두 축을 다 올려도 35** — 챔피언(37) 미달. 임팩트 4·오리지널리티 4가 다음 표적 |
| 17 BASIS | **31**/40 | **오리지널리티 5**·모션 5 — 히어로→CTA 틀을 처음 깼다(히어로가 3번째, 큰 클로징 CTA 없음) / 인주 레드를 회계 기호로만 사용, "수수료 하락=이득"일 땐 레드 억제 / 5개 기관 세후이자 직접 검산 일치 | 결함 11건 중 8건 수정, 3건은 근거와 함께 반려. **약점: 컬러 3·설득력 3·완성도 3** |
| 18 CUMUL | **32**/40 | **오리지널리티 5**·모션 5 / **390px가 캐논, 데스크톱이 펼침면** — 17개 RUN 중 첫 모바일 우선 / 고도 스케일을 UI에 결합 | 결함 11건 수정완료. **약점: 컬러 3·완성도 3** → 이번 배치 최고점 |
| 19 BASELINE | **31**/40 | 모션 5·완성도 4 / "정상범위가 아니라 당신의 기준선" — 판독 문서를 페이지 형식으로 / 항목칩+연도 슬라이더가 그래프와 판독 문장을 함께 바꿈 | 결함 14건(최다) 수정완료. **약점: 오리지널리티 3** — 형식은 새로웠으나 실행이 관습적 |
| 20 BEVEL | **30**/40 | 모션 5 / 스크럽 없이 **상태 동기화**로 밀어붙인 컨피규레이터 PDP — 옵션이 가격·중량·납기·비교표를 동시 갱신 | 결함 13건 수정완료. **약점: 여백 3·컬러 3·완성도 3** |
| 21 TANSO | **28**/40 | 스탯 밴드를 히어로 앞으로 보낸 구조 / 사양 카탈로그 아카이타입 | **이 RUN의 크리틱이 학습 7줄을 낳았다** — 꼬리 4모듈이 RUN16과 동일해 오리지널리티 3에 묶임, --mut AA 미달, 사진↔캡션 불일치(CNC 캡션 아래 석유화학 플랜트) |
| 22 ANCRE | 미채점 | 히어로에 광고 대신 **실시각으로 도는 CSS 시계** / 4축 구성이 시계 그림과 예상 일차·개체번호를 함께 바꿈 | 워크플로우 주문(4개) 밖 에이전트 자율 산출물 — 독립 크리틱 점수 귀속 불가. 브라우저 검증만 통과 |
| 27 외발 OEBAL | *자체 36*/40 ⚠ | **독립 크리틱 미실시 — 자체 채점**(같은 단서는 RUN29 행 참조). 근거로 확인된 것만: **여백 표적 달성** — 7섹션 상하 패딩이 `s6/s4 · s10/s7 · s7/s10 · s9/s5 · s5/s9 · s8/s6 · s10/s10` 으로 **같은 조합이 하나도 없다**(두 RUN 연속 3점이던 칸) / **AD-3 유서경 don't 4종 실측 0건** — 본문 17px·행간 1.64~1.98(최저값도 1.5 초과), 모든 섹션에 본문, 사진 5장 전부 캡션, 패딩 전부 다름 / **조작형이 지면 전체를 다시 짠다** — 지종 4종을 누르면 바탕·먹색만이 아니라 `line-height 31.45→27.88px` · `letter-spacing .085→-.068px` · 괘선 `1→2px` 가 함께 움직인다(실측). 「얇고 잘 번지는 종이일수록 행간을 넓혀야 한다」는 실제 조판 논리를 인터랙션의 근거로 삼았다 / **수치가 완전히 닫힌다** — 지종별 초지 1,160+1,720+880+420=4,180장, 도련 반려 1,045÷4,180=25.0%("넉 장 중 한 장" 본문과 일치), 납품 4,180−1,045=3,135장 / 제3자 인용 3건(실명·소속·날짜·출처 표기) / **닫는 방식이 판권(colophon)** — 27장 중 처음, 큰 전환 블록 없음 | 자가 대조 6문항에서 **5번(제3자 트러스트) 불합격**을 스스로 잡아 인용 3건을 추가했다 — 구술은 자사 장인의 말이라 트러스트가 아니다. 390에서 표는 `tr`·`td`까지 `display:block` 으로 내려 327px까지 접힌다(RUN26 학습 적용). **남은 약점: 임팩트 3 — 편집 지면이라 조용한 것이 의도지만 「스크롤을 멈추게」 하는 장면이 없다.** 다음 표적 |
| 29 돋을 DOTEUL | *자체 36*/40 ⚠ | **독립 크리틱 미실시 — 자체 채점이라 그대로 믿을 수 없다**(이 저장소 기록상 자체 35·37 → 독립 크리틱 28~32). 근거로 확인된 것만: 하드코딩 `font-size` 0개·46곳 전부 토큰, 중간 단계(`lg`·`d2`·`d3`)를 비워 본문 16 ↔ 디스플레이 184 극단 대비 / **AD-2 don't 4종 실측 0건**(`box-shadow` 0·`blur` 0·`border-radius` 0·3열 균등 0) / 포인트 원색 1색을 「켜지는 것」에만 — 낮 상태에서는 `--k`를 아예 쓰지 않는다 / **조작형 2종**(낮↔밤 하드 전환 + 간판 담기/빼기 → 벽면·비교표 동시 재구성), 슬라이더 회피 / 수치 상호 검산 612÷1,284=48%, 594÷612=97% / 실명·상호·업종·지역 리뷰 + 평점 4.8(213건) + 시공 크레딧 6곳 / **닫는 방식이 「우리가 만들지 않는 것」 거절 목록** — 큰 CTA 없음, 27장 중 처음 / 섹션 시퀀스가 직전 두 RUN과 0겹침 | 결함 2종 자체 발견·수정(벽면 상단 127px 빈 띠 — `align-items:flex-end`+`min-height`로 "벽"이 빈 상자로 읽힘 · **네 섹션 전부 `s9/s9` 동일 패딩**). 헤드리스 390 오버플로는 **오탐**으로 판명(위 ★학습). **남은 약점: 임팩트 4 — 히어로 우하단이 비었고, 간판 판 안쪽 여백이 과하다.** 다음 AD-2 슬롯은 32번 |
| 26 울림통 ULLIMTONG | **32**/40 → 2차 재검 **31**/40 (수정 후) | **임팩트 5·모션 5** — 620px "132" (**정정: 1차 기록의 「네 폭 모두 화면 밖으로 나감」은 사실이 아니었다** — 2차 크리틱 글리프 실측 820 −1px · 1024 −7px · 1440 −187px · 1920 −432px 로 지면 안에 멈춰 있었다. `--t-d3` 를 `min(56vw,1100px)` 로 바꿔 390~1960px 전 구간에서 실제로 잘려 나가게 고쳤고, 스펙 박스도 이제 숫자 잉크를 115~170px 덮는다) / ★크로스오버 드래그가 대역·옥타브·부하를 재계산하고 경고 밴드를 뒤집으며, **그 상태가 다음 섹션의 담당 유닛까지 바꾼다(모듈 간 결합, 이 저장소 최초)** / 키보드 조작·포커스 복귀·`?capture=1` 전부 실측 통과 / **발명 수치 상호 정합 최고** — 96+41=137, 137÷0.42=326kg/㎡, 6600÷2400=2.7배, 400÷137=2.9, 원장 5행 합=11,200 / **AD-2 don't 4종 무위반 + 식별 성공** / KELVIN 꼬리 3모듈(스펙밴드·팩·다크 CTA) 전면 폐기, 「탈락 체크리스트」로 닫음 | 결함 9종 수정완료(390 원장표 비고 열 89px 뭉갬 · 종이 위 주황 12px 3.16:1 AA 미달 **— 자기가 토큰 주석에 쓴 금지 규칙을 자기가 어김** · 스윕 눈금이 균등 배치라 로그 막대와 최대 81px 어긋남 · 390에서 축 라벨 겹침 · tally "34곳"과 "11,200h(5곳 합)" 모순 · 앵커가 sticky 바 아래 착지 · 죽은 마크업 4개 + aria 범위 오류 · 사진 캡션 과장·라인어레이 표현 · reduced-motion이 340vh 핀을 안 품). **약점: 여백 3·완성도 3** — 다섯 섹션이 전부 `s8/s9` 동일 패딩<br>**② 2차 독립 크리틱 31/40 → 지적 12종 전부 수정.** 새로 나온 실결함: reduced-motion에서 스크럽 분모가 **−77px** 이 되어 값이 역주행(19kHz→32Hz) · 히어로 사진이 캡션과 다른 대상(소형 소비자 스피커의 LCD·노브가 보이고 크롭의 35%가 날아간 창) · 옥타브 점유율을 **「부하 %」** 로 출력해 18인치 우퍼를 30%로 표기 · 390px에서 닫는 섹션 질문 열이 **72px** 로 뭉개짐(원장표에서 고친 규칙을 같은 파일의 다른 3열에 적용하지 않음) · `role="img"` 이 내부 `role="slider"` 2개를 보조기술에서 삭제 · 연속 음압 132dB인데 전력은 피크만 표기 · 원장에 스펙 없는 품목 「서브」 등장 · 비율 3개 합이 99% · 캡처가 `scroll-margin-top` 80px 유지 · 스케일 밖 임의 padding 6곳 · 하드코딩 `#cfccc3`. **수정 결과:** 다섯 섹션 호흡을 56/120·40/56·120/32·32/120·80/56 으로 갈라놓고, 스윕에 `.live`/`.fold` 두 벌을 만들어 폴백에서 3대역을 세로로 펼치며, 스펙시트를 연속 126dB·피크 132dB / 연속 700W·피크 2,400W 로 정합화(감도 98dB/W 로 양쪽이 맞는다) · 서브우퍼 TONG-S18 별매 정의 추가 · 히어로 사진을 손으로 짠 야외 스택으로 교체(캡션도 그 대상으로) |
| 25 밤길 BAMGIL | **28**/40 | 아트디렉터 제도 도입 후 **첫 don't 무위반**(라운드 2px·그림자 0·풀블리드 사진 위 흰 글씨 0·그레인 0) — 크리틱이 결과물만 보고 AD-1을 지목함 / **발명 수치의 상호 정합 최고 수준** — `first + headway×n = last`가 세 노선 모두 정확히 떨어져 11·9·6회, 25정류장=9+8+8, 노선도와 표가 같은 데이터원 / `.wrap` 겸용 섹션 padding 사고가 **처음으로 재발하지 않음**(9/9 실측 24px) / 인터랙션 종류 신규 — 다이어그램 노드 클릭으로 **표의 기준점을 재설정** | 결함 10종 수정완료(고정 viewBox가 글자를 3.7px↔31.5px로 왜곡 · 인라인 `1/13`이 6칼럼 폴드 무효화 · `figure` UA 마진이 도판을 격자 밖으로 · `--mut` 줄무늬 위 4.22:1 AA 미달 + 주석값 오류 · 클릭타깃 10px·데드존 · Enter 후 포커스 소실 · capture가 lazy 이미지 안 풂 · 푸터 "시각표"가 노선도로 감 · 비고 열 전 행 공백 · 낮 시간대 상태 부재) + 콘텐츠 3종(N3 정류장 순서 역전 · 요금이 "할증 없음"과 불일치 · 34석 과다). **약점: 컬러 3 · 임팩트 3 · 설득력 3 · 완성도 3** → 설득력은 「이 안내의 근거」(인가·정시율·이용·민원 + 출처) 신설로 대응. **베이스 28에서 새 아카이타입을 또 세운 것이 패착 — RUN23이 이미 낸 결론(총점 높은 것을 리믹스)을 쓰지 않았다** |
| 15 KERN | **35**/40 | 타이포 위계 5·모션 5(T3 가로 스크럽+라이브 테스터) / 신규 아카이타입으로 이전 RUN과 섹션 0겹침 / 라이선스 표 설득력 | 결함 4종 수정완료(한글 word-break·웹폰트 미로드·71개국 줄바꿈+라벨 불일치·smooth가 스크롤복원 가드 무력화). **남은 약점: 비주얼 임팩트 4(페이퍼&잉크라 조용함)·설득력 4(제3자 트러스트 신호 0)** → RUN16은 이 둘만 노려 37 |
