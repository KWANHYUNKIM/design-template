# 이 저장소는 어떻게 굴러가는가

랜딩 템플릿을 **한 번에 찍어내지 않는다.** 한 장 만들고 → 채점하고 → 다음 장이 그 점수를 넘으려 한다.
그 반복을 자동화한 것이 이 문서가 설명하는 **디자인 진화 루프**다.

다양성만으로는 좋아지지 않는다. 무작위로 25장을 뽑으면 25장의 평범한 결과가 나올 뿐이다.
매 RUN이 **평가받고, 배운 것을 문서에 남기고, 다음 RUN이 그것을 읽고 시작**해야 점수가 복리로 쌓인다.

---

## 1. 전체 그림

```
사용자: "다음 거 만들어줘"
        │
        ▼
  ┌───────────────┐
  │  design-loop  │  ← 스킬이 자동 발동
  └───────┬───────┘
          │  ① EVOLUTION.md · art-directors.md · workflow · 구성 대장 먼저 읽기
          ▼
   아트디렉터 배정 → 방향 결정 → 컨셉 발명 → 이미지 검증
          │                        ⟵ taste
          ▼
   구성 선언 관문 (닫는 방식·꼬리·시퀀스) ──조건 6개 중 하나라도 걸리면──▶ 다시 짠다
          │ 통과
          ▼
   HTML 생성                                  ⟵ animate
          │
          ▼
   verify.mjs (13종 자동 검사)  ──오류 있으면──▶ 고쳐서 재검사
          │ 오류 0
          ▼
   브라우저 3폭 검증 (390 / 1024 / 1440)      ⟵ claude-in-chrome (MCP)
          │
          ▼
   독립 크리틱 채점 (8기준 × 5점 = 40점)      ⟵ 서브에이전트
          │                                     만든 주체가 채점하지 않는다
          ▼
   결함 수정 → EVOLUTION.md에 점수·학습 append → 챔피언 갱신
          │
          ▼
  ┌────────────────┐
  │ design-release │  ← 갤러리·기록·인스타·배포
  └───────┬────────┘
          │
          ▼
   피그마 디자인 시스템 반출                  ⟵ figma (플러그인 + MCP)
```

두 개의 프로젝트 스킬이 릴레이한다. **design-loop**이 만들고 채점하면, **design-release**가 세상에 내보낸다.
그 둘이 중간에 **claude-in-chrome**과 **figma**를 불러 쓴다 — 자세한 층 구분은 다음 절에.

---

## 2. 스킬 인벤토리 — 세 개의 층

이 루프를 굴리는 건 프로젝트 스킬 4개지만, 실제로 이 세션에 떠 있는 스킬은 **33개다.**
어디에 사는지에 따라 세 층으로 갈린다. **층이 다르면 따라다니는 범위가 다르다.**

| 층 | 사는 곳 | 개수 | 저장소를 클론하면 |
|---|---|---|---|
| ① 프로젝트 | `.claude/skills/` | 4 | **따라온다** (git에 커밋돼 있다) |
| ② 플러그인 | `~/.claude/plugins/` | 13 | 안 따라온다 — 각자 설치 |
| ③ 내장 | Claude Code 자체 | 16 | 처음부터 있다 |

### ① 프로젝트 스킬 — 이 저장소에만 있는 것

| 스킬 | 발동어 | 하는 일 |
|---|---|---|
| **design-loop** | "RUN 만들어줘", "다음 거", "루프 돌려줘" | 템플릿 1장 생성 → 검사 → 채점 → 학습 기록 |
| **design-release** | "갤러리 갱신", "배포해줘", "인스타 카드" | 갤러리 카드·제작 기록·포스팅 자산·Pages 배포·피그마 연결 |
| **taste** | "AI 티 난다", "방향 잡아줘", "톤 정해줘" | 만들기 전 미감 확정 · 만든 뒤 진부함 대조 |
| **animate** | "모션 넣어줘", "인터랙션", "너무 정적이야" | 기법 중복 회피 · 조작형 인터랙션 설계 · 폴백 4종 |

design-loop은 4개 파일로 쪼개져 있다. 본문은 짧게 두고, 규격과 채점표는 필요할 때만 읽도록 분리했다.

- `SKILL.md` — 루프 11단계 절차
- `references/composition.md` — 구성 대장. 닫는 방식 · 증언 자리 · 골격 관용구
- `references/spec.md` (97줄) — 산출물 규격 + **함정 23종**
- `references/scoring.md` (53줄) — 8기준 채점 세부 기준

**taste**와 **animate**는 루프의 ④(컨셉 발명)와 ⑦(생성)에서 갈라져 나온 것이다.
둘 다 이 저장소의 25 RUN 기록을 근거로 삼는다 — 일반론이 아니라 **여기서 실제로 감점당한 목록**이다.

- `taste` — 진부함 목록이 "흔해서 나쁘다"가 아니라 **여기서 이미 써서 점수가 안 나온 것들**이다.
  만들기 전 4문장 확정, 만든 뒤 자가 대조 6문항
- `animate` — 25개 템플릿의 **기법 대장**을 들고 있다.
  IntersectionObserver 리빌은 28/28이 썼고, **`<input type="range">` 슬라이더는 7번 썼다**(15·16·17·19·20·21·23).
  그래서 슬라이더는 이제 새 인터랙션이 아니다

**이 네 개만 저장소에 커밋된다.** 남이 이 저장소를 클론하면 루프 절차·채점 기준·미감 판단·기법 대장까지
그대로 따라오지만, 아래 두 층은 각자의 환경에 달려 있다.

### ② 플러그인 스킬 — 계정 전역

`~/.claude/plugins/cache/claude-plugins-official/` 에 설치돼 있고, 어느 저장소에서 작업하든 뜬다.

| 플러그인 | 버전 | 내용 |
|---|---|---|
| **figma** | 2.2.91 | 스킬 12종 + MCP 서버 |
| **frontend-design** | Anthropic | 스킬 1종 — 템플릿 기본값처럼 보이지 않는 UI를 만드는 지침 |
| **playwright** | Microsoft | MCP 서버 (스킬 없음) — 헤드리스 브라우저 자동화 |
| **typescript-lsp** | 1.0.0 | LSP 도구 (스킬 없음) · 이 저장소는 TS를 안 써서 미사용 |

**frontend-design은 `taste`와 겹치지만 층이 다르다.** frontend-design은 "어떤 프로젝트에서든"
통하는 일반 지침이고, `taste`는 **이 저장소가 25 RUN 동안 실제로 감점당한 목록**이다.
방향이 안 잡힐 때 frontend-design을 먼저 읽고, 구체적인 판단은 `taste`로 내린다.

figma 스킬 12종은 방향에 따라 갈린다.

- **코드 → 피그마:** `figma-use`(모든 쓰기의 필수 선행) · `figma-generate-design` · `figma-generate-library` · `figma-create-new-file`
- **피그마 → 코드:** `figma-design-to-code` · `figma-implement-motion` · `figma-code-connect`
- **맥락별:** `figma-use-figjam` · `figma-use-slides` · `figma-use-motion` · `figma-generate-diagram` · `figma-swiftui`

이 루프가 쓰는 건 **코드 → 피그마 방향 3개**뿐이다. 템플릿의 원본은 언제나 코드이기 때문이다.

### ③ 내장 스킬 — Claude Code 자체

설치 없이 처음부터 있는 것들. 이 저장소와 관계있는 것만 추리면:

| 스킬 | 이 루프에서의 쓰임 |
|---|---|
| `claude-in-chrome` | **⑨ 브라우저 검증의 실행부.** 3폭 리사이즈·스크린샷·콘솔 읽기 |
| `code-review` · `simplify` | `verify.mjs`·`make-posts.mjs` 같은 스크립트를 고칠 때 |
| `dataviz` | 차트가 들어가는 RUN을 만들 때 (팔레트·축·범례 규칙) |
| `artifact-design` · `artifact-diagramming` | 산출물을 공유 가능한 페이지로 낼 때 |
| `run` | 로컬 서버를 띄워 결과를 확인할 때 |
| `loop` · `schedule` | 루프를 사람 개입 없이 반복 실행할 때 |

나머지(`init` · `security-review` · `update-config` · `keybindings-help` · `fewer-permission-prompts` ·
`claude-api` · `artifact-capabilities` · `figma-*` 중복분)는 이 저장소에선 거의 쓰이지 않는다.

참고로 **개인 스킬 층(`~/.claude/skills/`)은 비어 있다.** 이 환경에서 개인용 스킬은 만들지 않았다.

### 도구 층 — MCP 서버 2개

스킬은 "무엇을 어떤 순서로"를 적은 문서고, 실제로 손을 움직이는 건 도구다.

| MCP 서버 | 도구 | 루프에서 |
|---|---|---|
| **claude-in-chrome** | `navigate` · `resize_window` · `computer` · `javascript_tool` · `read_console_messages` | ⑨ 브라우저 검증 |
| **playwright** | 헤드리스 브라우저 자동화 · 스크린샷 · 폼 조작 | 3폭 캡처를 스크립트로 반복할 때 |
| **figma** | `use_figma` · `generate_figma_design` · `get_variable_defs` · `get_screenshot` | 배포 단계의 디자인 시스템 반출 |

**브라우저가 둘인 이유.** claude-in-chrome은 **내가 보고 있는 실제 크롬 탭**을 조작한다 —
눈으로 확인하는 ⑨ 검증에 맞다. playwright는 격리된 헤드리스 브라우저를 띄운다 —
**같은 검사를 반복 실행**할 때(3폭 캡처, RUN 여러 개 일괄 확인) 맞다.
지금까지는 `--headless=new --screenshot` 을 Bash로 직접 때렸는데, 그 자리를 대체할 수 있다.

MCP 도구 호출 권한은 `.claude/settings.local.json` 의 allowlist에 쌓인다.
매번 승인 팝업을 띄우지 않으려고 반복 명령을 거기에 등록해 둔 상태다.

### 루프가 실제로 부르는 것

| 단계 | 부르는 것 |
|---|---|
| ①~③ 읽기·디렉터·방향 | design-loop |
| ④~⑤ 컨셉·이미지 | design-loop + **taste** (방향이 안 잡히면 `frontend-design` 먼저) |
| ⑥ 구성 선언 (관문) | design-loop + `references/composition.md` 구성 대장 |
| ⑦ 생성 | design-loop + **animate** (모션·인터랙션 설계) |
| ⑧ 자동 검사 | `node scripts/verify.mjs` — 스킬 아님, 그냥 스크립트 |
| ⑨ 브라우저 검증 | **claude-in-chrome** MCP (반복 캡처는 **playwright**) |
| ⑩ 독립 크리틱 | 서브에이전트 (`Agent`) — 별도 컨텍스트로 띄운다 |
| ⑪ 기록·커밋 | design-loop |
| 배포 | **design-release** + `figma-use` · `generate_figma_design` |

`taste`와 `animate`는 design-loop이 **부르는** 것이지 대체하는 게 아니다.
루프의 ④·⑦이 두꺼워져서 떼어낸 것이고, 단독으로도 발동한다 —
이미 만든 템플릿을 두고 "AI 티 난다"거나 "너무 정적이야"라고 하면 그 스킬만 뜬다.

---

## 3. 루프 11단계

### ① 시작 전에 반드시 읽는 4개 파일

이 루프의 전부라고 해도 된다. 읽지 않고 시작하면 **같은 실수를 반복한다.**

- `EVOLUTION.md` — 현재 챔피언 · 검증된 학습 30여 줄 · 약점 패턴 · 점수 이력
- `art-directors.md` — 아트디렉터 3종의 do / don't
- `design-automation-workflow.md` — 구조 프리셋 · 체크리스트 · 함정 모음
- `references/composition.md` — 구성 대장. 닫는 방식 · 증언 자리 · 골격 관용구

### ② 아트디렉터 배정

`RUN 번호 % 3` 으로 기계적으로 배정한다. 연속 두 RUN이 같은 디렉터를 받지 않는다.
배정된 디렉터의 **do와 don't를 둘 다** 지켜야 하고, don't 위반은 완성도 −1이다.
디렉터 이름은 화면에 노출하지 않는다.

### ③ 방향 결정 — 홀짝

- **홀수 RUN = 새 방향.** 아카이타입(페이지의 종류 자체)을 이전 RUN들과 다르게 발명한다
- **짝수 RUN = 챔피언 리믹스.** 계승하되 강화한다. 골격만 재사용하면 오리지널리티가 정체된다
- 리믹스 대상은 "약점이 뚜렷한 것"이 아니라 **총점이 높은 것**을 고른다

> RUN 23은 목표한 두 축(컬러·완성도)을 3→5로 정확히 올렸는데도 35에서 멈췄다.
> 베이스로 고른 CUMUL이 32였기 때문이다. 챔피언 37을 골랐어야 했다.

### ④ 컨셉·콘텐츠 발명

브랜드·서사·섹션 주제·카피·수치·리뷰를 **직접 창작**한다. 템플릿 채우기가 아니다.
발명한 수치는 현실적이어야 하고, **단위와 라벨의 의미가 일치**해야 한다.
`71개국`(국가) 옆에 라벨 `지원 언어`(언어)를 붙이면 읽는 순간 가짜 티가 난다.

### ⑤ 이미지 — 컨택트시트로 눈으로 본다

`curl 200`은 "뜨는가"만 보장하고 **"그 이미지가 맞는가"는 보장하지 않는다.**
후보를 한 페이지에 모아 실제 피사체를 확인한다.

> 이 단계를 건너뛰어서 터진 사고들:
> "CNC 3축 밀링" 캡션 아래 **석유화학 플랜트 전경**(RUN21),
> 갯벌 안전 앱 후보 12장 중 **콜로세움·열대 리조트·사막 도로**(RUN23).

기존 RUN이 쓴 사진 ID와 겹치지 않게 대조한다.

```bash
grep -oh "photo-[a-zA-Z0-9_-]*" templates/*.html | sort -u
```

### ⑥ 구성 선언 — 관문

**27장을 실제로 대조해서 생긴 단계다.** 브랜드·카피·수치는 매번 새로 발명되는데
골격은 수렴했다 — 마지막 섹션이 전환 블록인 것이 **24장**, 증언 섹션이 **20장**,
리빌이 `IntersectionObserver`+`.rise` 그대로인 것이 **27장 전부**.
「증언 → 가격/FAQ → 닫기」 3연은 최근 12장 중 8장이 썼다.

이 사실은 이미 세 군데에 적혀 있었다 — `EVOLUTION.md` 약점 패턴, RUN21 교훈,
`SKILL.md` "점수가 막혔을 때". **세 번 다 문장이었고, 세 번 다 무시됐다.**
그래서 HTML을 한 줄도 쓰기 전에 통과해야 하는 관문으로 옮겼다.

선언하는 것: 섹션 역할 시퀀스 · 닫는 방식 · 증언의 위치 · 리빌 외 주 모션 ·
섹션 헤더 변주 · 직전 3개 RUN과의 최장 연속 일치.

**통과 조건 6개** — 하나라도 걸리면 생성으로 넘어가지 못한다.

| # | 조건 | 근거 |
|---|---|---|
| 1 | 닫는 방식이 CTA 밴드·뉴스레터·문의/예약/주문 폼이 **아니다** | 이 셋을 27장 중 21장이 썼다 |
| 2 | 꼬리 3섹션 조합이 직전 3개 RUN과 일치하지 않는다 | RUN21이 여기서 오리지널리티 3에 묶였다 |
| 3 | 직전 3개 RUN과 연속 일치 구간이 2개 이하 | 순서 파괴가 히어로에서만 일어나는 걸 막는다 |
| 4 | 「증언 → 가격/FAQ → 닫기」 3연을 쓰지 않는다 | 최근 12장 중 8장 |
| 5 | IO 리빌이 유일한 모션이 아니다 | 27 / 27 |
| 6 | 라벨+`h2`+lead 헤더가 3회 이하 | RUN17 이후 파일당 4~10회 |

> 대장은 `.claude/skills/design-loop/references/composition.md` 에 있고,
> **RUN이 끝날 때마다 갱신한다.** 갱신을 빼먹으면 다음 RUN이 같은 걸 또 쓴다 —
> 이 관문이 무력해지는 유일한 경로다.

이미 깬 전례가 있다. RUN24는 판권(콜로폰)으로, RUN25는 이용 안내로,
RUN26은 「사기 전에 답해야 하는 다섯 가지」 탈락 체크리스트로,
RUN29는 「우리가 만들지 않는 것」으로 닫았다. **넷 다 버튼 없이 닫았다.**

### ⑦ 생성

`templates/run-NN-<slug>.html` **자체완결 HTML 한 장.** 외부 CSS/JS 파일 없음.
빌드 도구 없이 브라우저에서 바로 열려야 한다.

핵심 규격은 `:root` 토큰이다. 색·타입 스케일(모듈러 1.25배)·간격 스케일(4배수)을 토큰으로 박고,
**모든 `font-size`는 `var(--t-*)` 참조. 하드코딩 px 0개가 합격 기준이다.**

### ⑧ 자동 검사

```bash
node scripts/verify.mjs run-NN
```

**오류 0이 될 때까지 고친다.** 다만 이건 앞단만 거른다 — 통과했다고 끝이 아니다.

### ⑨ 브라우저 검증 (건너뛰지 말 것)

지금까지 잡은 결함의 **절반 이상이 여기서만 보였다.**

```bash
python3 -m http.server 5500
```

- **390 / 1024 / 1440 세 폭**을 직접 본다
- 인터랙션을 실제로 조작해 화면이 바뀌는지 본다
- `?capture=1` 모드가 펼쳐지는지 본다
- 이미지가 `loading="lazy"`면 강제로 eager로 바꿔 확인한다

> 검사기는 만들기 **전에** 지킬 규칙을 검사한다. 도판 붕괴·스탯 오버플로·모바일 고아 칸·
> 사진↔캡션 불일치는 만든 **뒤에** 봐야 보인다. RUN21은 규칙을 다 지키고도 이 넷이 전부 터졌다.

### ⑩ 독립 크리틱 채점

**만든 주체가 채점하지 않는다.** 자기 작품을 자기가 후하게 보는 걸 막는 것이 이 장치의 핵심이다.
서브에이전트로 띄우고, "근거 없는 칭찬은 감점"을 명시해 `grep`·`curl`·계산으로 근거를 잡게 한다.

> 자체 채점한 RUN15·16은 35·37이 나왔고, 독립 크리틱이 붙은 RUN17~22는 28~32가 나왔다.
> 같은 잣대가 아니었을 가능성이 높다.

### ⑪ 결함 수정 → 기록 → 커밋

- 크리틱이 지적한 것만 고친다. 지적이 틀렸다고 판단되면 고치지 말고 **근거를 밝힌다**
- `EVOLUTION.md` 점수 로그에 append. 챔피언 초과 시 챔피언 교체
- **새로 배운 것을 "검증된 학습"에 추가** — 이게 복리로 쌓이는 부분이다
- 무엇을 왜 바꿨는지 커밋 메시지에 남긴다

---

## 4. 채점 — 8기준 × 5점 = 40점

| # | 항목 | 5점은 무엇인가 | 흔한 감점 |
|---|---|---|---|
| 1 | 타이포 위계 | 모듈러 스케일 위에 있고 위계가 한눈에 읽힌다. 하드코딩 px 0개 | 눈대중 사이즈 · 치환 후 위계가 평평해짐 |
| 2 | 컬러 & 대비 | 포인트 1색을 절제해 쓰고 규칙이 있다. 대비 실측 통과 | 순백 배경 · 포인트색 남발 · AA 미달 |
| 3 | 여백 & 리듬 | 간격이 스케일 위에 있고 섹션 호흡이 기계적이지 않다 | 모든 섹션이 같은 패딩 · 임의 px |
| 4 | 비주얼 임팩트 | 스크롤을 멈추게 하는 장면이 있다 | 조용함 · 남의 사진에만 기댐 |
| 5 | 모션 & 인터랙션 | 2종 이상 혼합 + 조작하면 화면이 바뀌는 것 1개 + 폴백 완비 | 리빌 한 종류 · 값만 바뀌는 슬라이더 |
| 6 | 콘텐츠 설득력 | 수치가 현실적이고 서로 검산되며, 제3자 트러스트 신호가 있다 | 자체 작성 카피만 · 비현실 스펙 |
| 7 | 오리지널리티 | 아카이타입·모듈 순서가 기존과 다르고 AI 티가 없다 | 히어로→CTA 틀 답습 · 꼬리 모듈 재사용 |
| 8 | 완성도 & 버그 | 3폭 검증 통과 · 폴백 완비 · 결함 0 | 함정 23종 중 하나라도 위반 |

### 추가 판정 3종

| 항목 | 판정 |
|---|---|
| **디렉터 준수** | 배정된 AD의 don't를 하나라도 어겼는가 → **완성도 −1** |
| **디렉터 식별** | 결과물만 보고 어느 AD인지 맞힐 수 있는가 → 못 맞히면 **오리지널리티 상한 3** |
| **구성 대조** | ⑥에서 선언한 것을 결과물이 실제로 지켰는가. 크리틱이 직접 직전 3개 RUN의 섹션 시퀀스를 뽑아 대조한다 → 닫는 방식이 소진 3종이거나 꼬리 3섹션이 기존과 일치하면 **오리지널리티 상한 3** |

두 번째가 이 장치의 핵심이다. **"잘 만들었지만 누가 만들었는지 모르겠다"**가 지금까지의 문제였다.
세 번째는 그 짝이다 — **"매번 다른 브랜드인데 같은 페이지처럼 읽힌다"**가 나머지 절반이었다.
선언(⑥)과 채점(⑩)이 같은 조건을 보기 때문에, 관문을 말로만 통과하고 다른 걸 만들면 여기서 걸린다.

---

## 5. 자동 검사기 — 13종

`scripts/verify.mjs`. 각 검사는 실제 RUN에서 한 번씩 터진 결함이다.

### 오류 (하나라도 있으면 불합격)

| 검사 | 안 지키면 |
|---|---|
| `hardcoded-font-size` | 타입 위계가 눈대중이 되어 4점에서 막힌다 |
| `keep-all` | "디스플레이 산세리 / 프."처럼 단어 중간이 끊긴다 |
| `webfont-link` | 이름만 적으면 그 폰트가 깔린 내 맥에서만 보인다 |
| `img-onerror` | 이미지 실패 시 빈 칸. 그라디언트 폴백 필수 |
| `scroll-guard` | `scroll-behavior:smooth`가 스크롤 복원 가드를 무력화한다 |
| `wrap-padding-shorthand` | `.wrap` 겸용 섹션의 좌우 패딩을 덮어써 그 섹션만 어긋난다 |
| `reveal-class-name` | `.in` 같은 흔한 이름은 레이아웃 클래스와 충돌해 div가 grid로 변한다 |
| `reduced-motion` | 모션 접근성 폴백 부재 |
| `mobile-fallback` | 900px 이하에서 sticky pin이 살아 있으면 빈 500vh 구간이 생긴다 |
| `pipeline-meta-leak` | 화면에 RUN 번호·프리셋명·티어가 새면 즉각적인 AI 티 |

### 경고

`capture-mode`(`?capture=1` 유무) · `grid-min-width`(그리드 자식 `min-width:0`) · `countup-unit-span`(카운트업 단위 span 분리)

> `.wrap` 겸용 섹션 padding 단축 금지는 **RUN16에서 직접 EVOLUTION에 적은 규칙인데 RUN23에서 6개 섹션 전부 재발했다.**
> 그래서 규칙을 문서에만 두지 않고 검사기에 넣었다. 문서에 쓰는 것과 지키는 것은 다르다.

---

## 6. 왜 점수가 오르는가 — 실제로 통한 것

루프가 굴러가면서 검증된 학습이 `EVOLUTION.md`에 30여 줄 쌓였다. 그중 점수를 실제로 움직인 것들:

- **정체를 뚫은 건 기법이 아니라 아카이타입 교체였다.** RUN13·14는 T2 모션 기법을 늘려도 30에 묶였다.
  RUN15가 페이지의 종류 자체를 "타입 스페시멘"으로 바꾸자 35로 뛰었다
- **낮은 두 칸만 공략한다.** RUN16은 임팩트·설득력만 노려 둘 다 4→5, 총점 35→37.
  매 RUN 전방위로 잘하려 하면 아무 칸도 안 오른다
- **트러스트는 "제3자가 한 말"이어야 점수가 된다.** 자체 작성 사용예는 설득력 4,
  이름·직함·소속이 붙은 리뷰 + 평점 + 사용 크레딧은 5
- **인터랙션은 결과가 눈에 보이게.** 슬라이더는 값만 바꾸지 말고 **화면을 바꿔야** 한다.
  그리고 직전 RUN과 **종류**가 달라야 한다 — 조작 대상만 바꾼 같은 물건은 새 인터랙션이 아니다
- **꼬리를 재사용하지 마라.** 히어로만 새로 짜고 마지막 4모듈이 같으면 순서 파괴는 무효다.
  "순서를 깬다"는 모듈 하나를 앞으로 옮기는 게 아니라 **페이지가 닫히는 방식을 바꾸는 것**이다.
  **이건 세 번 문장으로 적혔다가 세 번 무시돼서, ⑥ 관문의 통과 조건 2·3번이 됐다**

---

## 7. 현재 상태

| 항목 | 값 |
|---|---|
| 누적 RUN | 25 |
| 현재 챔피언 | **RUN 16 · KELVIN — 37 / 40** |
| 도전 실패 | RUN 17~24 전부 미달 (최고 RUN23 들물 35) |
| 챔피언의 남은 약점 | 오리지널리티 4 · 타이포 위계 4 · 여백 리듬 4 |
| 검증된 학습 | 30여 줄 누적 |

38점을 넘으려면 **구조를 계승하되 순서를 깨야 한다.** 골격이 직계 리믹스인 한 오리지널리티는 4에서 막힌다.

---

## 8. 릴레이 — design-release

채점과 기록이 끝나면 두 번째 스킬이 받는다.

1. `index.html` 갤러리에 카드 추가 · 템플릿 카운트 갱신
2. `log.html` 제작 기록에 RUN 카드 추가 (점수·강점·개선점)
3. `node scripts/make-posts.mjs` — 인스타 포스팅 카드 · 캡션 생성
4. GitHub Pages 배포 확인
5. `figma-links.json` 에 피그마 링크 연결

---

## 파일 지도

### 저장소 안 — git에 커밋되는 것

```
.claude/
  skills/
    design-loop/
      SKILL.md              루프 11단계
      references/composition.md  구성 대장 — 닫는 방식·증언 자리·골격 관용구  ← ⑥ 관문의 근거
      references/spec.md    산출물 규격 + 함정 23종
      references/scoring.md 8기준 채점표
    design-release/
      SKILL.md              배포·홍보 절차
    taste/
      SKILL.md              미감 방향 + 진부함 목록
    animate/
      SKILL.md              기법 대장 + 인터랙션 설계 + 폴백 4종
  settings.local.json     도구 권한 allowlist (커밋 제외)

EVOLUTION.md              챔피언 · 검증된 학습 · 점수 로그   ← 루프의 기억
art-directors.md          아트디렉터 3종 do/don't
design-automation-workflow.md  구조 프리셋 · 체크리스트

scripts/verify.mjs        13종 자동 검사
scripts/make-posts.mjs    인스타 카드 생성

templates/run-NN-*.html   산출물 (자체완결 1파일)
index.html                공개 갤러리
log.html                  제작 기록
operation.md              이 문서 (원본)
operation.html            이 문서를 읽어 렌더하는 뷰어
```

### 저장소 밖 — 환경에 딸린 것

```
~/.claude/
  skills/                 개인 스킬 — 비어 있음
  plugins/
    cache/claude-plugins-official/
      figma/2.2.91/skills/       스킬 12종 + MCP 서버
      frontend-design/skills/    스킬 1종
      playwright/                MCP 서버 (npx @playwright/mcp)
      typescript-lsp/1.0.0/      LSP 도구
    installed_plugins.json       설치 목록

(내장) code-review · simplify · dataviz · run · loop · schedule ·
       artifact-* · claude-in-chrome · init · security-review 등 16종
```

**클론한 사람에게 따라가는 건 위쪽 블록뿐이다.** 루프의 절차·규격·채점 기준·학습·미감 판단·기법 대장·구성 대장은
전부 저장소 안에 있어서 그대로 재현되지만, 피그마 연결과 브라우저 검증은 각자 플러그인과 MCP를
붙여야 동작한다. 그 둘이 없어도 **①~⑧과 ⑩~⑪은 그대로 굴러간다.**
