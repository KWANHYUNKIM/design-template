# 두 스킬을 어떻게 쓰는가

이 저장소에는 디자인을 만드는 방식이 **둘** 있다. 하는 일이 아예 다르다.

| | `design-loop` | `design-clone` |
|---|---|---|
| 한 줄 | **없는 걸 만든다** | **있는 걸 그대로 옮긴다** |
| 목표 | 매번 다른 물건 | 원본과 같은 물건 |
| 시작 | 브리프(무엇을 왜) | 레퍼런스(URL) |
| 창작 | 브랜드·카피·수치 전부 발명 | **창작 금지.** 재현만 |
| 멈춤 | 승인 지점 **2개** | **없음.** 끝까지 자동 |
| 성공 | 독립 크리틱 점수가 오름 | 원본과의 차이가 0에 가까움 |
| 산출 | `templates/run-NN-*.html` | `templates/ref-NN-*.html` |

## 어느 걸 쓸지 — 한 문장으로 갈린다

**"참고할 화면이 있는가?"**

- 있다 → `design-clone`. URL을 주면 그대로 옮긴다
- 없다 → `design-loop`. 무엇을 왜 만드는지만 주면 발명한다

말로 부르는 법은 이렇다.

```
"토스처럼 만들어줘"        → design-clone   (참고 대상이 있다)
"이 사이트 복제해줘"        → design-clone
"랜딩 하나 더 만들어줘"     → design-loop    (참고 대상이 없다)
"다음 RUN 돌려줘"          → design-loop
```

---

# ① design-loop — 없는 걸 만든다

## 그래프

```
[브리프] → N1 팬아웃(3안) → ◆승인1 → N2 종합 → N3 검증 → ◆승인2 → N4 출력
                                            └─(미달만, 최대 2회)─┘
```

**두 군데서 멈춘다.** 시안 3개를 보여주고 고르라고 한 번, 최종 확정 전에 한 번.

## 무엇을 주면 되는가

```
만들 것   : 랜딩 1장 / 로고 / 카드뉴스
목적      :
타깃      :
톤앤매너  :
필수 요소 : 브랜드 컬러·로고·수치
금지 사항 : 그라데이션 금지 등
```

빈칸이 있으면 **시작 전에 묻는다.** 이 저장소에서 돌릴 때는
`EVOLUTION.md` 의 챔피언·검증된 학습·약점 패턴이 브리프에 자동으로 얹힌다.

## 각 노드에서 일어나는 일

**N1 팬아웃** — 방향이 뚜렷이 다른 시안 3개를 낸다.
색만 바꾼 셋은 한 안이다. 아카이타입(페이지의 종류 자체)이 달라야 한다.

> 이 저장소는 **아트디렉터 3인**이 이미 팬아웃 장치다.
> 한서린(스위스 그리드) · 도진(브루탈리스트) · 유서경(편집디자인).
> 셋을 동시에 쓰면 A·B·C가 자연히 갈린다.

**◆승인1** — 여기서 멈춘다. `"B로 가줘"` 또는 `"A 레이아웃 + C 컬러"` 처럼 조합해도 된다.

**N2 종합** — 고른 방향으로 완성안 1개. 이때 `taste`(미감)와 `animate`(모션)가 붙는다.

**N3 검증** — 네 항목을 자체 평가하고 **미달만** 고쳐 다시 돈다. 최대 2회.

| 항목 | 본다 |
|---|---|
| 필수 요소 | 브리프의 필수가 다 들어갔는가 |
| 금지 사항 | 브리프 금지 + 배정된 AD의 don't |
| 톤 | `taste` 진부함 목록에 걸리는가 |
| 가독성 | 대비 실측 AA · 390/1024/1440 세 폭 |

**◆승인2** — 최종 확정 전 멈춤.

**N4 출력** — 최종안 + 주요 결정 3가지 + `EVOLUTION.md`·`log.html`·갤러리 기록 + 커밋.

## 실제로 나온 것 — RUN 29 「돋을」

사인 공방. 브랜드·제품·가격·후기가 전부 발명이다.

- 배정 AD: 도진(브루탈리스트) — 라운드 0 · 그림자 0 · 3px 실선 · 극단 스케일
- 조작형: **낮↔밤 전환.** 밤을 켜면 비발광 간판이 배경에 묻혀 사라진다
- 닫는 방식: 큰 CTA 없이 **「우리가 만들지 않는 것」** 거절 목록
- 자체 채점 36/40 (독립 크리틱 미실시로 명시)

---

# ② design-clone — 있는 걸 그대로 옮긴다

## 그래프

```
[공유 상태] → N1 스펙추출 → N2 재현 → N3 비교검증 ─(차이 남음, 최대 3회)─┐
                                  │                                    │
                                  └─(차이 없음)→ N4 출력  ◄─────────────┘
```

**멈추지 않는다.** N4까지 자동으로 가고, 노드가 끝날 때마다 한 줄만 표시한다.

```
■ N1 스펙추출 완료 → N2
■ N2 재현 완료 → N3
■ N3 1회차 : 레이아웃 3곳 미달 → 재실행
■ N3 2회차 : 전 축 통과 → N4
```

## 무엇을 주면 되는가

```
레퍼런스   : URL (또는 이미지)
결과물 형식 : HTML/CSS · Figma · 컴포넌트
복제 범위   : 전체 페이지 / 히어로만 / 특정 화면
출력 모드   : 연구용 / 공개용
```

**빈칸이 있어도 묻지 않는다.** 기본값으로 채우고 N4에서 무엇을 가정했는지 밝힌다.
자동 실행이 이 그래프의 전제라서다. 정확히 지정하고 싶으면 처음에 다 적어 주면 된다.

## 각 노드에서 일어나는 일

**N1 스펙추출** — 브라우저로 원본을 열어 **재고 걸어 다닌다.**

| 재는 것 | 예 |
|---|---|
| 레이아웃 | 섹션 y좌표·높이·그리드 열수·최대폭 |
| 컬러 | HEX와 **사용 빈도**, 포인트색이 쓰인 자리 |
| 타이포 | 크기·굵기·행간·자간 |
| 간격 | padding·margin 빈도 분포 |
| 컴포넌트 | 카드·버튼 개수와 라운드 |
| 상태 | 클릭하면 뭐가 열리는가 |

> 이게 이 스킬의 전부다. **눈으로 보고 흉내내면 내 손맛이 섞인다.**
> 재서 옮겨야 남의 시스템이 된다.

**N2 재현** — N1 스펙**만** 근거로 만든다. 기억이나 관행으로 채우지 않는다.
**회색 빈 상자로 뼈대를 먼저 완성**하고 좌표가 맞은 뒤에 콘텐츠를 넣는다.

**N3 비교검증** — 같은 측정 스크립트를 산출물에 돌려 원본과 표로 대조한다.
차이 나는 부분만 고쳐 다시 돈다. 최대 3회.

**N4 출력** — 최종 결과물 + **원본과 불가피하게 다른 부분과 그 이유.**
`"안 맞았지만 못 고쳤다"` 와 `"일부러 안 했다"` 를 구분해서 적는다.

## 실제로 나온 것 — REF 07 「온담」

LG전자 홈을 복제했다. 뼈대는 같고 브랜드는 발명이다.

- 원본 뼈대 **17섹션 · 9,033px · 컨테이너 1380px** 를 실측
- **좌표 17/17 통과** — 3회 반복해 맞췄다(11/17 → 14/17 → 17/17)
- 오차 0%가 8개: 히어로 500 · 퀵메뉴 82 · 프로모션 224 · 얇은배너 110 …
- 6화면을 한 파일 뷰 스위칭으로: 홈 · GNB드롭다운 · 목록 · 상세 · 장바구니 · 검색
- 사진 14장은 무료 이미지를 원본 비율 슬롯에 맞춰 넣었다

---

# 둘 다에 걸리는 선

## 복제할 때 안 가져오는 것

- ❌ 로고·워드마크·브랜드 심볼
- ❌ 실제 제품명
- ❌ 그 회사의 사진·아이콘 원본
- ❌ 실제 카피 문장
- ❌ **그 회사인 것처럼 보이게 하는 모든 것**

**뼈대·비율·좌표·시스템은 가져온다.** 이건 어느 회사의 소유물도 아니고
디자인 교육이 원래 이렇게 굴러간다. 가져오지 않는 건 **브랜드 정체성**이다.

산출물이 공개 갤러리로 나가므로 `scripts/verify.mjs` 의 검사 3종이 자동으로 막는다 —
대상 브랜드명이 화면에 남거나, 그 회사 CDN을 참조하거나, 출처 주석이 없으면 오류다.

## 만들지 않는 화면

**로그인 · 결제 · 주문조회.** 자격증명이나 결제 정보를 받는 화면은 껍데기라도 만들지 않는다.
주문서까지는 만들되 결제 직전에서 멈춘다.

---

# 곁다리로 붙는 것들

메인 둘이 필요할 때 부르는 스킬들이다. 직접 부를 일은 거의 없다.

| 스킬 | 언제 붙는가 |
|---|---|
| `taste` | 만들기 전 미감 확정 · 만든 뒤 진부함 대조 |
| `animate` | 모션·인터랙션 설계. 이미 쓴 기법 대장을 들고 있다 |
| `design-ref` | 시스템(토큰)만 옮길 때. `design-clone` 의 앞단 |
| `design-release` | 갤러리·기록·인스타 카드·배포 |

`design-ref` 와 `design-clone` 의 차이가 헷갈리기 쉽다.

- **design-ref** — 값의 **분포**를 옮긴다. 같은 시스템, **다른 물건**
- **design-clone** — 구조의 **좌표**를 옮긴다. **같은 뼈대**, 다른 브랜드

토큰만 맞추면 안 닮고, 뼈대만 맞추면 싸구려 모조가 된다. **둘을 같이 쓴다.**

---

# 지금까지 쌓인 것

| 종류 | 개수 | 위치 |
|---|---|---|
| 창작 템플릿 | `run-01` ~ `run-38` | `templates/` |
| 복제 템플릿 | `ref-01` ~ `ref-11` | `templates/` |
| 측정한 시스템 | 6곳 | `refs/` |
| 검증된 학습 | 40여 줄 | `EVOLUTION.md` |

측정한 시스템 여섯을 나란히 놓으면 **"국내 테크 스타일" 같은 건 없다**는 게 보인다.

| 축 | 당근 | 우아한형제들 | 오늘의집 | LG전자 |
|---|---|---|---|---|
| 본문 | 14px | 22px | 14px | 14px |
| 타입 단계 | 4 | 10+ | 7 | 7 |
| 간격 주력 | 8·12 | 32 | 8 | 16 |
| 자간 | normal | −0.32 | −0.3 | **−0.2/−0.4/−0.5** |
| 포인트색 | UI 미사용 | UI 미사용 | 전면 사용 | 텍스트 전용 |
| 그림자 | **0건** | 1건 | 7건 | 16건 |
| 모션 | 0.15s | 0.3s | 0.1s | 거의 없음 |

거의 모든 축에서 다르다. **레퍼런스를 바꾸면 결과물이 실제로 갈라진다** —
이게 `design-clone` 을 만든 이유다.
